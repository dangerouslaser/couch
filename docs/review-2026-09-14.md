# Review task list (2026-09-14)

**Status 2026-09-14 evening:** 22 tasks done in PRs #142–#145 (marked ✅ below); second batch in progress: T-018, T-019, T-023, T-039, T-041, T-092, T-093, T-129–T-134, T-168, T-169.

Eight parallel code reviews of the app repo (daemon, GUI, web UI, clients and model, boot and OS scripts, tools/release/CI), the kernel repo and the landing page, consolidated and deduplicated into one list. Each task cites its source report; the reports themselves are not in the repo. Work the list from the suggested execution order at the end; strike tasks through or move them to a Done section as PRs land.


Merged and deduplicated from eight code-review reports: `daemon.md`, `gui.md`, `web.md`,
`clients-model.md`, `tools-release-ci.md`, `boot-os.md`, `kernel.md`, `site.md`.

173 tasks (172 open, 1 already done). Every task traces to at least one report; overlapping
findings are merged into a single task and cite every report they came from.

Fields: **ID** · title · area · category (debt / optimization / feature) · severity · effort ·
`file:line` references · what is wrong and what to do · source report(s).

Sections are ordered by (severity, then smallest effort) except section 8, which is ordered by
value to a universal-remote user as requested.

---

## 1. Fix now — correctness & safety bugs

### T-001 — ✅ DONE #142 · Clear the recovery BCB on power off
**area** daemon · **category** debt · **severity** high · **effort** S
`daemon/couch-system/src/power.rs:67-77`, `daemon/couch-system/src/service.rs:219`,
`initramfs/init:43-44`, `initramfs/init:49-74`, `recovery/init:11-15`
`Action::Off` is an empty match arm, so powering the remote off during the ~95–150 s window in
which init leaves `boot-recovery` armed strands the next boot in recovery with no explanation.
Treat `Off` exactly like `Restart` (PR #138 fixed only `Restart`/`UpdateRestart`), and split the
decision into a pure `fn bcb(action) -> Bcb` so `power::tests` can assert it.
*Reports: daemon 1.1, boot-os 2 (a)*

### T-002 — ✅ DONE #142 · Make an interrupted credential migration recoverable
**area** daemon · **category** debt · **severity** high · **effort** S
`daemon/couch-confd/src/store.rs:106`, `store.rs:128`, `store.rs:76`, `main.rs:57`
Both migration temporaries use a fixed name with `create_new(true)` and are never removed on
error, so one battery pull leaves `hue-connection.migrate-new` behind and every later
`Store::open` fails `AlreadyExists` → `exit(1)`: the web UI is gone until someone gets a shell.
Use the pid-suffixed temp + remove-on-error shape of `staging::atomic` / `access.rs:69`, and add a
test that pre-creates the leftover temp.
*Report: daemon 1.2*

### T-003 — ✅ DONE #143 (validated on hardware in .149.dev: keeper stopped by pid) · Write the backlight-keeper PID after `/tmp` is mounted
**area** boot-os · **category** debt · **severity** high · **effort** S
`initramfs/init:185-192`, `initramfs/init:200-202`, `stage2/gui-start.sh:54-67`
The PID is written to the initramfs `/tmp` ten lines before a tmpfs is mounted over it, so
`gui-start.sh` can never read it and always falls back to the `keeper_scan()` heuristic PR #135
added the file to replace (#134/#135). Move the keeper block below the tmpfs mount or write to
`/run/backlight-keeper.pid`, and add a `tools/tests` case asserting the file is readable after the
mounts.
*Report: boot-os 1*

### T-004 — ✅ DONE #144 · Validate scene and activity step commands, and fix `Action::suggestions`
**area** model · **category** debt · **severity** high · **effort** S
`model/couch-model/src/validate.rs:207-216`, `validate.rs:236-242`, `validate.rs:243-250`,
`model/couch-model/src/device.rs:259-273`, `model/couch-model/src/commands.rs:46-88`,
`daemon/couch-confd/src/api.rs:781`, `ui/couch-gui/src/activity_buttons.rs:340`
Button bindings are command-checked; scene and activity steps check only that the device exists,
so a step saves and then fails at press time. Worse, the model's own `suggestions` advertises
`dim:30`, `bright`, `volume:20`, `open`, `target:18` — none of which `Function::parse` accepts —
and has no callers. Apply the binding closure to `scene.steps` and `activity.steps`, delete or
rewrite `suggestions`, and add a test that every string it can emit parses.
*Report: clients-model TD-6*

### T-005 — ✅ DONE #143 (rollback path not yet exercised on hardware) · Give `portal.sh` a rollback trap and stop its httpd
**area** boot-os · **category** debt · **severity** high · **effort** M
`stage2/portal.sh:41-52`, `:76`, `:105`, `:123`, `:135-136`, `:159`, `stage2/station.sh:7`,
`daemon/couch-system/src/cgi.rs:39-49`
After line 41 the station is torn down and the radio is in AP mode; every later failure is a bare
`exit 1` leaving no station, no AP that hands out leases, and `/tmp/couch.setup` present. Nothing
ever kills the `busybox httpd`, so after a join it keeps serving `cgi-bin/scan` (ungated) on the
home LAN. Add `cleanup()` + `trap cleanup EXIT` that kills hostapd/dnsmasq/httpd, restores STA
mode, removes the marker and re-runs `station.sh`; record the httpd pid so `station.sh` kills it.
*Report: boot-os 3*

### T-006 — ✅ DONE #142 · Route the update restart through `power::perform`
**area** daemon · **category** debt · **severity** med · **effort** S
`daemon/couch-system/src/service.rs:236`, `daemon/couch-system/src/power.rs:77`,
`service.rs:219-227`
The update path calls `busybox reboot -f` with no `sync`, discarding the GUI's config writes,
`settings.conf` and logs. Call `power::perform(Action::Restart, BCB)` instead — it already does
the BCB clear these lines hand-roll, plus the sync.
*Report: daemon 1.8 (a)*

### T-007 — ✅ DONE #142 · Skip a release with no asset digest instead of failing the whole check
**area** daemon · **category** debt · **severity** med · **effort** S
`daemon/couch-updates/src/release.rs:182`, `release.rs:229`
`listed(...)?` propagates out of the per-release loop, so one release whose `update.json` asset has
no GitHub digest turns the entire update check into `phase: "error"` and hides every older, signed
release. Return `Ok(None)` and log, as the missing-asset branch four lines above already does.
*Report: daemon 1.8 (b)*

### T-008 — ✅ DONE #145 · Derive the "nothing is over the hub" predicate once
**area** gui · **category** debt · **severity** med · **effort** S
`ui/couch-gui/src/main.rs:854-857`, `:1051-1056`, `:1205`, `:742-755`, `:767-769`,
`ui/components/settings.slint:204`
Three hand-written copies with 12, 10 and 7 terms. Because `on_home` omits
`thermostat_shown`/`camera_shown`, holding Menu over the thermostat or camera opens Settings on
top of it; because the reload gate omits five terms, a config revision from the web UI rebuilds
the hub models under an open screen. Add one `fn overlay(app) -> Option<Overlay>` and derive all
five call sites (plus `exit_device`/`back_context`) from it.
*Report: gui TD-4*

### T-009 — ✅ DONE #145 · Report a dropped activity-button command
**area** gui · **category** debt · **severity** med · **effort** S
`ui/couch-gui/src/activity_buttons.rs:90-106`, `:69`, `ui/couch-gui/src/main.rs:876`, `:1349-1350`
`fire()` does `let _ = try_send(...)` and returns `true`, so on a full 8-deep queue the remote's
primary input silently vanishes — every other dispatch path (`tv.rs:1099`, `activity.rs:564`,
`lights.rs:565`, `shortcuts.rs:125`) reports it. Return the send result and raise
`Feedback::Error("Still sending the last command")`, which already becomes a toast.
*Report: gui TD-5*

### T-010 — Publish the real room status columns
**area** gui · **category** debt · **severity** med · **effort** S
`ui/couch-gui/src/home.rs:60-83`, `:155-171`, `:249-269`,
`ui/screens/home_hub.slint:370`, `:389-391`, `:321-323`
`status_known`, `active_count`, `idle`, `offline` and `dimmed` are hardwired constants on real
hardware, so the entire right-hand status column, the `"N ON"` text and the `"OFFLINE"` branch
never render and every card stays in the recessed idle style. Have `RoomMonitor` publish
`(power_state, on_count, known)` and an `offline` signal from a failed Hue/HA fetch.
*Report: gui TD-7*

### T-011 — ✅ DONE #145 (no-config screen not yet seen on hardware) · Show an empty/error state instead of the fabricated demo house
**area** gui · **category** debt · **severity** med · **effort** S
`ui/couch-gui/src/main.rs:126-213`, `:231`, `:1082`,
`ui/couch-gui/src/config_snapshot.rs:34-37`, `ui/couch-gui/src/qr.rs`
If `config.json` is missing or fails validation at boot there is no snapshot, so the user sees four
fictional areas on which every OK press does nothing, with only an `eprintln!` as the signal. Show
"No configuration yet — open the web UI at <address>" using the existing `net_web` string and QR
renderer, keep the demo behind `COUCH_DEMO=1`, and toast a rejected update.
*Report: gui F-5*

### T-012 — ✅ DONE #143 · Fix the initramfs serial-shell backup respawn guard
**area** boot-os · **category** debt · **severity** med · **effort** S
`initramfs/init:156-164`
`init` itself runs as `busybox sh /init`, so `pgrep -f "busybox sh"` matches init and every
backgrounded subshell; the "second, independent" respawn loop therefore `continue`s forever and
the promised redundancy on the only way back into a wedged device does not exist. Record the
primary loop's pid and test `[ -d /proc/$PRIMARY ]`, or use a `/tmp/serial-shell.pid`.
*Report: boot-os 6*

### T-013 — ✅ DONE #143 · Delete the premature `$PROPS` extraction in `hardware-init.sh`
**area** boot-os · **category** debt · **severity** med · **effort** S
`stage2/hardware-init.sh:81-83`, `:100-105`, `:133`, `:93`, `:167-169`
`$PROPS` is first assigned 85 lines later, so line 82 is `tar xzf ""` into an empty tmpfs and line
83 reports `props 0` as if it measured something — directly contradicting the comment that
justifies the wmt-detection ordering. Delete lines 81-82, drop the `props` field from the report,
and add `set -u` (with `${COUCH_DEBUG:-}` / `${PORTAL_TIMEOUT:-}`) to the stage2 scripts.
*Report: boot-os 4*

### T-014 — ✅ DONE #143 · Wait for `wlan0` instead of sleeping three seconds
**area** boot-os · **category** debt · **severity** med · **effort** S
`stage2/hardware-init.sh:231-240`, `:189-193`, `:264-268`, `:282-286`, `stage2/stage2.sh:26-28`
Every other wait in this file polls for the event and says why; whether `wlan0` exists at all is
decided by one fixed `sleep 3`. A slow probe means `wlan0 MISSING`, no supplicant, no DHCP, and a
perfectly configured device dropped into local onboarding. Poll `[ -d /sys/class/net/wlan0 ]` for
~10 s at 0.25 s and record the elapsed time in the S6 marker.
*Report: boot-os 5*

### T-015 — ✅ DONE #143 (validated on hardware in .149.dev) · Move `touch /tmp/stay` inside the GUI-exists branch
**area** boot-os · **category** debt · **severity** med · **effort** S
`stage2/gui-start.sh:33`, `:82`, `initramfs/init:109`
`/tmp/stay` is touched before the `[ -x "$GUI" ]` test, so a runtime with no GUI binary disarms the
900 s dead-man and then fails the health gate instead of being caught by either.
*Report: boot-os 9*

### T-016 — ✅ DONE #144 · Teach `release_discovery.py` the real tag scheme
**area** tools-release-ci · **category** debt · **severity** med · **effort** S
`tools/installer/release_discovery.py:18`, `:78`, `tools/installer/couch_tui.py:259`,
`tools/installer/test_release_discovery.py:13`, `daemon/couch-updates/src/release.rs:21-33`
The `VERSION` regex accepts `v0.1.0-alpha.5` but not `v0.1.0-alpha.20260913.137`, so the TUI's
"Browse GitHub releases" reports nothing for the real repository, and the fixtures all use the old
scheme so no test notices. Port `release.rs::accepts` semantics (including `.dev`) or widen the
regex, and use real-shaped tags in fixtures.
*Report: tools-release-ci TD-7*

### T-017 — ✅ DONE #144 · Fix `wait` and `killall` in the Wi-Fi debug supervisor
**area** tools-release-ci · **category** debt · **severity** low · **effort** S
`tools/installer/wifi-stage/debug-supervisor:58`, `:64`, `:19-24`, `:68-76`,
`docs/installer-review-followups.md:37-44`
`$BB wait` is not a BusyBox applet, so `result` is permanently 127 and every lifecycle line reports
`worker_exit=failure` while the worker is never reaped; the `killall` only runs while the worker is
alive, so a supplicant-socket timeout leaves the old `wpa_supplicant` holding the control socket.
Drop the `$BB` prefix on line 64 and hoist the `killall` past the loop.
*Report: tools-release-ci TD-11*

---

## 2. Reliability & security debt

### T-018 — Consolidate the pinned-certificate TLS verifier into the SDK
**area** clients · **category** debt · **severity** high · **effort** M
`clients/couch-webos/src/tls.rs:11-65`, `clients/couch-androidtv/src/tls.rs:11-65`,
`clients/couch-tizen/src/tls.rs:16-70`, `clients/couch-hue/src/tls.rs:23-77`,
`clients/couch-unifi-protect/src/pinning.rs:24`, `couch-webos/src/lib.rs:95-130`, `:527-552`,
`couch-tizen/src/lib.rs:201-236`, `:613-640`
Five copies of a trust-on-first-use verifier differing only in an error string, plus two verbatim
copies each of the `Socket` enum and Wake-on-LAN. Add `couch_sdk::tls` (`Pin`,
`pinned_client_config`, `Socket`) and `couch_sdk::wol`; ~350 lines deleted and the one existing
`changed_certificate_is_rejected` test covers every TLS client.
*Report: clients-model TD-1*

### T-019 — Replace the nine hand-rolled credential writers with `couch_sdk::save_private`
**area** clients · **category** debt · **severity** high · **effort** M
`clients/couch-sdk/src/settings.rs:52-127`, `:98-105`; callers
`couch-ha/src/settings.rs:22`, `couch-hue/src/settings.rs:23`, `couch-kodi/src/settings.rs:23`,
`couch-tizen/src/settings.rs:81`, `couch-webos/src/settings.rs:54`, `couch-webos/src/power.rs:76`,
`couch-control/src/streaming.rs:98`, `couch-appletv/src/metadata/store.rs:52`,
`couch-matter/src/lib.rs:181`, `:179-197`
Eight use a collision-prone `{pid}.new` temp (two saves in one process collide and surface as
"Invalid Samsung TV configuration"); `couch-matter` uses `create/truncate` and never fsyncs the
parent directory. Substitute the SDK helpers, which already carry pid+nanos+sequence.
*Report: clients-model TD-2*

### T-020 — Stop `/api/health` leaking every interface to unpaired callers
**area** daemon · **category** debt · **severity** med · **effort** S
`daemon/couch-confd/src/api.rs:494`, `daemon/couch-confd/src/local_name.rs:164-194`
The unpaired branch returns `local_name::status()`, i.e. every non-loopback IPv4 interface with
name, address, raw `/sys` flags, multicast join result and up to 49 captured mDNS log lines — more
revealing than the revision counter it deliberately hides. Reduce the unpaired reply to
`{host, advertised, port}` and keep `interfaces`/`error`/`log` behind the paired branch.
*Report: daemon 1.3*

### T-021 — Rate-limit PIN challenge minting after a burn
**area** daemon · **category** debt · **severity** med · **effort** S
`daemon/couch-confd/src/auth.rs:156`, `:187`, `daemon/couch-confd/src/api.rs:288`, `:314`
Five wrong answers destroy the challenge and the very next `POST /api/auth/challenge` mints a fresh
one instantly, with no delay, no memory and no log line — so the "make the remote flash a PIN a
thousand times" defence costs a couple of minutes on a LAN, in another room. Add
`consecutive_burns` + `blocked_until` with `min(2^burns, 300)` s and a `retry_after`, reset on a
successful pair, and print on each burn.
*Report: daemon 1.4*

### T-022 — Make `settings.conf` writes durable and its booleans consistent
**area** daemon · **category** debt · **severity** med · **effort** S
`daemon/couch-system/src/ui_settings.rs:140`, `:97`, `:108`,
`daemon/couch-confd/src/store.rs:187-209`, `daemon/couch-system/src/access.rs:68-85`
The only "atomic" write in the tree with no `sync_all`, no directory fsync, no mode and no temp
cleanup — and it carries `ssh=` and `bluetooth=`, so a rename that lands without its data silently
turns "SSH off" into "SSH on if a key is enrolled". Also `keys` parses `v != "0"` while `ssh`
parses `v == "1"`. Give `save_to` the `access::atomic` body and pick one boolean rule.
*Report: daemon 1.5*

### T-023 — Chmod the Bluetooth HID socket and split the crate into lib + bin
**area** clients · **category** debt · **severity** med · **effort** S
`clients/couch-bt-hid/src/main.rs:501`, `:25`, `:13`, `:63-89`,
`ui/couch-gui/src/activity_buttons.rs:396-405`, `clients/couch-control/src/lib.rs:466`,
`model/couch-model/src/buttons.rs:202-225`
The datagram socket is bound in `/tmp` with no permissions (any local process can drive a paired
TV's power and volume) while `couch-control` explicitly sets 0600; the module doc says `/run` and
the code says `/tmp`; there is no lib so the GUI embeds the wire protocol and paths; and the crate
has zero tests, including none over the 22-entry `consumer_usage` table. Split lib/bin, chmod 0600
after bind, and assert `consumer_usage` is total over the `BluetoothTv` catalog.
*Report: clients-model TD-8*

### T-024 — Decode HTTP errors once in `api::ha` and surface them
**area** web · **category** debt · **severity** med · **effort** S
`web/couch-web/src/api.rs:249-260`, `:197-228`, `src/screens/protect.rs:19`,
`src/screens/remote_device.rs:79-83`, `src/screens/remote.rs:14-17`, `src/screens/updates.rs:56-61`
Every integration endpoint goes through `ha()`, which drops the status code and the server's text:
a 502 with an HTML body reads as "The remote returned an invalid response", and `stale` is never
set so 409/404 never triggers the reload the config path does. Four call sites then discard even
that with `if let Ok(..)` / `let _ =`. Factor `decode_error(status, text)` out of `parse()` and use
it from both; turn the four sites into `match` arms.
*Report: web 4*

### T-025 — Add browser coverage for the three largest web screens
**area** web · **category** debt · **severity** med · **effort** S
`web/couch-web/src/screens/activity_buttons.rs` (293 lines), `activity_sequences.rs` (176),
`activity_pages.rs` (87), `src/screens/remote_device.rs:161-164`,
`web/tests/browser.mjs:139-142`, `web/tests/area-shortcuts.mjs`
`docs/activities.md:38` and `docs/activity-buttons.md:70` claim browser coverage that greps to zero
hits; the Bluetooth toggle is untested too. Add `web/tests/activities.mjs` on the
`area-shortcuts.mjs` template: one short-press mapping, one long-press, "Use activity default", one
on-sequence command plus a delay and a reorder, one custom page with one button (~60 lines).
*Report: web 6*

### T-026 — Pin and verify `provision-alpine.sh` downloads
**area** tools-release-ci · **category** debt · **severity** med · **effort** S
`tools/provision-alpine.sh:37-48`, `:10`, `tools/release/clean_stage.py:88`,
`tools/fetch-zig.sh:24-29`, `docs/releases.md:21`
No `--fail` (an HTTP error page lands in the rootfs tarball and fails obscurely inside `tar`), no
checksum at all — while the release path pins this exact file's SHA-256 — and package names scraped
from an HTML index with `grep | head -1`. Reuse the pinned hash and `fetch-zig.sh`'s verify
fallback, add `--fail --proto '=https' --tlsv1.2`, and convert the `set -e`-only scripts
(`build-webui.sh:13`, `build-voice.sh:11`, `build.sh:3`, `build-recovery.sh:8`, `flash.sh:14`, …)
to `set -eu` with a `case "${1:-}"` guard.
*Report: tools-release-ci TD-10*

### T-027 — Give the GUI supervisor crash-loop backoff and escalation
**area** boot-os · **category** feature · **severity** med · **effort** S
`stage2/gui-start.sh:83-87`, `initramfs/init:49-74`
`couch-gui` is restarted forever with a flat 2 s delay, no crash-loop detection and no escalation,
and the health file it feeds is only consulted for the first ~150 s — so a runtime that degrades at
hour six loops silently. Add a restart counter that backs off and, after N restarts in M minutes,
re-arms the BCB and reboots.
*Report: boot-os 16.5*

### T-028 — Test the broker's timing, lease and dispatch behaviour
**area** clients · **category** debt · **severity** med · **effort** M
`clients/couch-control/src/lib.rs:340-346`, `:369-376`, `:182`, `:348-352`, `:247-303`,
`clients/couch-control/src/streaming.rs:226-319`, `clients/couch-control/src/proxies.rs` (0 tests),
`clients/couch-sdk/src/client.rs:129`
The four existing tests cover the documented fixtures; the 750 ms in-queue expiry, the post-connect
expiry, lane eviction at 128, credential-change invalidation and the whole `Op`→client dispatch
table are untested — and the broker's contract *is* its timing. Add an injectable clock (the code
already reads `job.at.elapsed()` twice) and a `catalog_differences`-style assertion over
`streaming.rs`'s three name→button tables.
*Report: clients-model TD-9*

### T-029 — De-shell and test the Bluetooth control paths
**area** daemon · **category** debt · **severity** med · **effort** M
`daemon/couch-system/src/bluetooth.rs:83`, `:88`, `:107-126`, `:151`, `:21`,
`daemon/couch-system/src/ui_settings.rs:209`
The only file in `couch-system` with no tests and the most shell-dependent: `pkill -f couch-bt-hid`
matches any command line containing the name, the bridge is killed by an `sh` loop forking `cat`
once per process, `down()` publishes `"off"` before checking whether the kills worked, and `auto()`
is documented dead code. Match on exact `comm`, signal with `libc::kill`, publish state only after
success, and test the pid-selection and state-publication logic with a fake process lister.
*Report: daemon 1.6*

### T-030 — Give recovery a bounded self-escape
**area** boot-os · **category** debt · **severity** med · **effort** M
`initramfs/init:72-74`, `recovery/init:11-15`, `docs/device-recovery.md:43-55`,
`stage2/stage2.sh:34`
Any boot where `couch-gui` is unhealthy for 150 s reboots with the flag armed, and recovery
deliberately never clears it — so a transient GUI crash loop is a permanent UI-less brick needing
USB serial or a pre-enrolled SSH key. Add a counter in `para` (or a marker sector) so the second
consecutive *unattended* recovery boot clears the flag and retries the normal slot, keeping the
"stay here" behaviour only when a serial session or an explicit request put it there.
*Report: boot-os 2 (b)*

### T-031 — Detect a Tizen pin mismatch from a flag, not an error string
**area** clients · **category** debt · **severity** low · **effort** S
`clients/couch-tizen/src/lib.rs:329-338`, `clients/couch-tizen/src/tls.rs:34`
A certificate mismatch is recognised by `io.to_string().contains("certificate")` — cross-crate
coupling through prose. Any rustls formatting change turns "pair again" into "TV connection
failed", sending the user to their network settings; and cert pinning is the first suspect for the
never-hardware-tested Tizen path. Have the shared `Pin` verifier (T-018) set a flag and check that.
*Report: clients-model TD-10*

### T-032 — Log refused control-socket connections
**area** daemon · **category** debt · **severity** low · **effort** S
`daemon/couch-system/src/service.rs:138`
Connections failing `root_peer` or exceeding the eight-connection cap are `continue`d with no reply
and no log line, so the client sees a bare EOF and the operator sees nothing.
*Report: daemon (also noted)*

### T-033 — Zero the Wi-Fi trial journal's cleartext PSK
**area** daemon · **category** debt · **severity** low · **effort** S
`daemon/couch-system/src/network.rs:225`, `daemon/couch-system/src/protocol.rs:77`, `:92`
`/tmp/couch-wifi-trial.json` holds the derived PSK in cleartext (0600, tmpfs) until save or cancel —
the one credential the protocol layer's `bytes.fill(0)` hygiene does not cover.
*Report: daemon (also noted)*

### T-034 — Make test scratch directories unique and always cleaned
**area** daemon · **category** debt · **severity** low · **effort** S
`daemon/couch-confd/src/api.rs:1110`, `daemon/couch-confd/src/store.rs:299`,
`daemon/couch-system/src/ui_settings.rs:267`
Several tests build scratch directories from `std::process::id()` alone and remove them only on the
success path, so a failing test leaves state that makes the next run fail differently.
*Report: daemon (also noted)*

### T-035 — Stop the shipped installer panicking on a malformed pin
**area** tools-release-ci · **category** debt · **severity** low · **effort** S
`tools/installer/host/src/public_inputs.rs:187-190`, `.github/workflows/firmware-watch.yml`
Four fields of the embedded `ha100_official_runtime.json` are `unwrap()`ed, so a malformed pin is a
panic in a shipped installer rather than a diagnosable error — and that pin is exactly the file
`firmware-watch.yml` proposes to change automatically.
*Report: tools-release-ci (also noted)*

---

## 3. Performance & power

### T-036 — ✅ DONE #142 · Make `process_running` cheap and stop calling it on GUI hot paths
**area** daemon · **category** optimization · **severity** high · **effort** S
`daemon/couch-system/src/ui_settings.rs:209-220`, `daemon/couch-confd/src/api/remote.rs:47`, `:58`,
`daemon/couch-system/src/bluetooth.rs:126`, `ui/couch-gui/src/main.rs:1139-1141`,
`ui/couch-gui/src/navigation.rs:114-135`
It `read_dir("/proc")` with no numeric filter and a `read_to_string` per entry: ~250 syscalls and
~120 allocations per call, run twice per `GET /api/remote/device`, once a second while Settings is
open, twice inside the settings page transition, and up to 30 times in `wait_for(hid_running)`
while holding the system gate. Filter to numeric entries, reuse the path buffer, read into a fixed
array, return on first match, and memo `bluetooth_state` for ~500 ms; gate the GUI's per-second poll
on `settings_panel == 7` rather than on the menu being open.
*Reports: daemon 2.1, gui TD-3*

### T-037 — ✅ DONE #145 · Move the SSH toggle and the settings-open probes off the UI thread
**area** gui · **category** debt · **severity** high · **effort** S
`ui/couch-gui/src/main.rs:644`, `:662-688`, `:566`, `:1126`, `ui/couch-gui/src/system.rs:176`,
`:192`, `:198`, `:148`, `daemon/couch-system/src/client.rs:7-27`,
`ui/couch-gui/src/navigation.rs:114-135`, `ui/couch-gui/src/updates_ui.rs:5`
`ssh_available/start/stop` are synchronous unix-socket round trips with a 100 s read timeout, and
`ssh_start` generates host keys — all inside a Slint callback, freezing frames, keypad and touch.
The Bluetooth toggle twenty lines below already uses a worker thread and `updates_ui.rs` documents
the pattern as "the way the SSH toggle already does". Give SSH the same channel + optimistic
"starting…" row, and move `ssh_available()` off the boot path and out of `Intent::OpenSettings`.
*Reports: gui TD-2, gui TD-3*

### T-038 — ✅ DONE #145 · Have `RoomMonitor` read the config snapshot instead of re-parsing the file
**area** gui · **category** optimization · **severity** high · **effort** S
`ui/couch-gui/src/home.rs:186-242`, `:194-207`, `:249`,
`ui/couch-gui/src/config_snapshot.rs:64-80`, `ui/couch-gui/src/connections.rs:9`
An unconditional flash read plus a full serde_json parse of `config.json` every 500 ms for the life
of the process, whether or not the file changed or the panel is even lit — while
`config_snapshot.rs` exists for exactly this and every other controller uses it. Use
`config_snapshot::current()`, keep the last-seen `serial`, recompute `has_ha`/`has_hue` and the
room→device index only when it moves, and early-return from `poll` unless a new map arrived.
*Report: gui OPT-1*

### T-039 — Stop holding the 3.3 MB IR index as a `Value` and rebuild the catalog per request
**area** daemon · **category** optimization · **severity** high · **effort** M
`daemon/couch-confd/src/api/ir.rs:197`, `:201`, `:226-252`, `:333-366`, `:389`,
`daemon/couch-confd/assets/ir/official-index.json`
The 3.3 MB official index plus a 240 KB catalog are parsed into a permanent `OnceLock<Value>` —
roughly 15–25 MB resident on a 1 GB no-swap device, with a transient double during the
`iter().cloned()` extend. `GET /api/ir/catalog` then walks every codeset and clones seven `Value`s
each on every request for a response that changes only when `/dev/irtx` appears. Deserialize the
index into a typed `Listed { id, brand, device_type, model, offset, length }`, and cache the
rendered catalog body, splicing in `blaster_available` per request.
*Report: daemon 2.2*

### T-040 — Stop cloning the whole `Config` inside web render loops
**area** web · **category** optimization · **severity** high · **effort** M
`web/couch-web/src/screens/activity_buttons.rs:183-218`, `:219-230`, `:237-238`,
`src/screens/rooms.rs:123`, `:133`, `:139`, `src/screens/areas.rs:41`,
`src/screens/activities.rs:93`, `:94`, `:116`, `activity_sequences.rs:8`, `activity_pages.rs:9,54`
`StoredValue::get_value()` clones: `mapping_label` is called twice per key for 23 keys (up to 46
whole-`Config` plus 46 `Activity` clones per render), the device options closure clones three more
times per device, `rooms.rs` clones three times per device row, and `activities::detail` clones five
times. Pass `&Config` or `StoredValue<Rc<Config>>`, hoist the single `get_value()` out of the loops,
and precompute the label vector once.
*Report: web 10*

### T-041 — Cache the Sonos client instead of connecting per keypress
**area** clients · **category** optimization · **severity** high · **effort** M
`ui/couch-gui/src/activity_buttons.rs:374-380`, `:326-329`, `:305-308`,
`clients/couch-sonos/src/lib.rs:566-624`, `:669`, `clients/couch-sonos/src/sdk.rs:108`
One volume-up is TCP connect + TLS handshake + `GET /players/local/info` + `GET
/households/local/groups` + the write, while Denon, webOS and streaming TVs all sit in caches in the
same function — and the press is abandoned after 750 ms, so Sonos is the provider most likely to
drop presses. Add `Spec::Sonos` to the broker (the `DeviceClient` impl exists) or, as a smaller
step, a `sonos: &mut HashMap<String, Client>` cache plus a short-TTL group-topology cache.
*Report: clients-model OPT-1*

### T-042 — Cache Matter CASE sessions and read lights concurrently
**area** clients · **category** optimization · **severity** high · **effort** M
`clients/couch-matter/src/lib.rs:355`, `:396`, `:423`, `:450`, `:471`, `:418-441`
`DeviceManager::connect` caches nothing, so every command pays a fresh Sigma/CASE handshake inside a
750 ms interaction budget; `lights()` then loops nodes serially at `OPERATION_TIMEOUT * 2`, so four
unreachable nodes stall the lights screen for 64 s. Keep an `LruCache<node_id, Connection>` with
idle eviction and `join_all` the per-node reads with a 4 s timeout.
*Report: clients-model OPT-2*

### T-043 — Start the GUI in parallel with Wi-Fi bring-up
**area** boot-os · **category** optimization · **severity** high · **effort** M
`stage2/stage2.sh:15`, `:18-22`, `:26-28`, `:34`, `:52`, `stage2/gui-start.sh:22-26`, `:82-88`,
`stage2/hardware-init.sh:189-193`, `:232`, `:264-268`, `:282-286`, `initramfs/init:49`
stage2 is strictly serial, so the user stares at the fbcon logo through up to 20 s STP + 3 s +
30 s association + 12 s DHCP before `couch-gui` starts — work the UI does not need
("Networking is not a health prerequisite"), and the health gate does not sample until t=90 s.
Keep the node/mount/vendor prologue serial, background the radio block, and re-evaluate the
setup-mode decision and `ssh-start` when the lease lands.
*Report: boot-os 11*

### T-044 — Read `COUCH_REGION` once instead of on every draw
**area** gui · **category** optimization · **severity** med · **effort** S
`ui/couch-gui/src/panel.rs:466-470`, `:527`, `ui/couch-gui/src/navigation.rs:46`
The lookup sits before `draw_if_needed`, so it runs up to 200×/s on an idle-but-lit screen, taking
the process-wide environment lock and scanning `environ` each time. Use a `OnceLock<bool>`, as
`navigation.rs` already does.
*Report: gui OPT-2*

### T-045 — Remove the per-frame heap allocations in the input path
**area** gui · **category** optimization · **severity** med · **effort** S
`ui/couch-gui/src/main.rs:767-770`, `:721-726`, `:1098`, `:1327`, `:1377`, `:1380`,
`ui/couch-gui/src/input.rs:191-196`, `ui/couch-gui/src/activity_buttons.rs:151-158`
A five-field `format!` back-context `String` and an `active_activity().to_string()` are allocated
and dropped once per main-loop iteration (60–200/s) purely to be compared; `feedback_page` (13
property reads) is evaluated four times per iteration and can dismiss twice for one change. Make
both contexts `Copy` values (bitmask + hash, or compare `SharedString`s) and compute
`feedback_page` once per iteration.
*Report: gui OPT-3*

### T-046 — Back off the `wifi::status()` poller when nobody is reading
**area** daemon · **category** optimization · **severity** med · **effort** S
`daemon/couch-system/src/wifi.rs:125`
`WORKER` spawns a thread that never stops and never slows: one supplicant round trip plus two
`String` allocations per second for the life of every process linking the crate — up to three on a
battery device. Record `last_read: AtomicU64` and back off to 15 s once nothing has read the cache
for 10 s, returning to 1 s on the next read.
*Report: daemon 2.4*

### T-047 — Stop `hold_key` blocking a broker lane for three seconds
**area** clients · **category** optimization · **severity** med · **effort** S
`clients/couch-tizen/src/lib.rs:519-524`, `:527-533`, `clients/couch-control/src/lib.rs:186-192`,
`:340-346`, `:369`, `clients/couch-control/src/streaming.rs:311`,
`clients/couch-denon/src/lib.rs:220`
A Frame TV power-off sleeps 3 s inside a single-threaded lane whose queue drops anything older than
750 ms, so pressing power discards every other command to that TV — including the volume keys the
user presses next. Allow a per-op deadline extension (as `Op::Open` already gets) or split into
`Press` + a scheduled `Release`; at minimum document the interaction beside the 750 ms rule.
*Report: clients-model OPT-4*

### T-048 — Fetch IR commands and input/app discovery once per activity
**area** web · **category** optimization · **severity** med · **effort** S
`web/couch-web/src/screens/device_commands.rs:25-49`,
`src/screens/activity_buttons.rs:36`, `:86-181`, `src/screens/activity_sequences.rs:6`, `:29-119`,
`src/screens/activity_pages.rs:7`, `src/screens/activities.rs:116-119`, `:123`, `:140-141`
`Commands::new` is constructed three times per activity page because all three tabs render eagerly
and two are hidden with `style:display`, and the inputs/apps `Effect` is duplicated between two
screens — six identical IR reads and four discovery calls with two IR devices, re-fired after every
save. Build `Commands` once in `detail()`, share one `dynamic_actions` helper, and render inactive
tabs lazily.
*Report: web 11*

### T-049 — Make the icon picker lazy and memoise its filter
**area** web · **category** optimization · **severity** med · **effort** S
`web/couch-web/src/ui.rs:87-119`, `:100`, `:109`, `:110`, `:119`,
`model/couch-model/src/icon_catalog.rs`
`matches()` walks all 2,077 icons three times per render and the 60-button grid is built eagerly
inside a closed `<details>`, so a room page with eight devices constructs 480 buttons and ~25,000
string comparisons before anything is opened — again after every save. Gate the grid on the
`<details>` `on:toggle` and compute `matches()` once into a `Memo`.
*Report: web 12*

### T-050 — Gate the 60× per-boot dmesg snapshot loop
**area** boot-os · **category** optimization · **severity** med · **effort** S
`initramfs/init:101-107`, `:91-100`, `:221-222`, `kernel/couch-ha100.config:116`,
`stage2/gui-start.sh:98`
Sixty passes of `dmesg` + `dd` of a 128 KB ring to the expdb partition — up to ~7.7 MB of eMMC
writes during the most contended 60 s of every boot, unlike the panel narration which is gated on
`/opt/couch/verbose`. Gate it on the same marker (or a dedicated one), drop the cadence to 5 s, and
default the shipped image to the single end-of-boot snapshot plus the heartbeat.
*Report: boot-os 10*

### T-051 — Bound the `/tmp` logs
**area** boot-os · **category** optimization · **severity** med · **effort** S
`stage2/system.sh:6-8`, `stage2/confd.sh:7-9`, `stage2/gui-start.sh:93`,
`stage2/hardware-init.sh:279`, `:272-277`, `initramfs/init:202`,
`tools/busybox/busybox.config:1223`, `:1225`
Three supervisors append forever into a tmpfs mounted with no `size=` (half of 1 GB), plus a
persistent `udhcpc` log for the whole uptime; nothing rotates, truncates or caps any of the 21
`/tmp/*.log` paths. Start `busybox syslogd -C512` (already compiled in, with `logread`) at the top
of stage2 and point the supervisors at `logger`, or truncate per iteration and cap with `tail -c 1M`.
*Report: boot-os 9*

### T-052 — Do not hold the global store mutex across `ir::save`
**area** daemon · **category** optimization · **severity** med · **effort** S
`daemon/couch-confd/src/api/device_ir.rs:181-187`
Both entry points hold the store `Mutex` across `ir::save`, which does two fsyncs on phone flash, so
every other API request waits behind it.
*Report: daemon (also noted)*

### T-053 — Let idle GUI workers block, and slow the idle main loop
**area** gui · **category** optimization · **severity** med · **effort** M
`ui/couch-gui/src/activity.rs:194`, `tv.rs:411`, `:427`, `activity_buttons.rs:223`, `:282`,
`activity_pages.rs:47`, `tv_media.rs:313,368,404,513`, `motion.rs:75`, `home.rs:241`,
`config_snapshot.rs:71`, `main.rs:1401-1404`, `:1424`, `keypad.rs:104`
About 20 worker threads spawn unconditionally and tick on 40–100 ms timers with no session open —
on the order of 100 wakeups/s on a battery handheld with the panel off — and the main loop spins at
200 Hz whenever the screen is lit and idle, running all ~18 polls each pass. Have session-less
workers `recv()` rather than `recv_timeout()` (as `tv.rs:427` already gates), and raise the idle
sleep to ~16 ms after 200 ms with no frame, or `poll()` the input fds.
*Report: gui OPT-4*

### T-054 — Make the discovery and Bluetooth endpoints asynchronous
**area** daemon · **category** optimization · **severity** med · **effort** M
`daemon/couch-confd/src/main.rs:34`, `daemon/couch-confd/src/api/tizen.rs:36-55`,
`api/streaming_tv.rs:87-130`, `:32-45`, `api/remote.rs:104`,
`daemon/couch-system/src/client.rs:21`, `daemon/couch-system/src/bluetooth.rs:107-126`,
`daemon/couch-confd/src/local_name.rs:212`
With only four HTTP workers, a Tizen search (3 s mDNS then up to 16 serial 2 s probes = 35 s), a
second `ServiceDaemon` in `streaming_tv`, and a Bluetooth toggle with a 100 s client timeout each
occupy a worker; two in flight and the page appears hung. Use the pending-session + reaper shape
the pairing endpoints already have (202 + token + poll), probe Tizen candidates concurrently, and
let the Bluetooth toggle return 202 and be polled through the existing state file.
*Report: daemon 2.3*

### T-055 — Stream the update download and extraction
**area** daemon · **category** optimization · **severity** med · **effort** M
`daemon/couch-updates/src/release.rs:88-111`, `:132`, `daemon/couch-updates/src/staging.rs:176`,
`daemon/couch-updates/src/boot.rs:260-263`, `:321`
The payload is read whole into a `Vec<u8>` with a 128 MiB cap and decompressed from that buffer,
and `boot::activate` holds three 16 MiB buffers plus the staged reads — around 60 MB peak, on a
1 GB no-swap device, during the worst possible moment. Feed a sink and the `Sha256` incrementally
into `slots/.staging-<pid>/payload`, extract from a `File`, and compare/write the boot partition in
page-sized chunks.
*Report: daemon 2.5*

### T-056 — Register the broker `Spec` once instead of re-serializing credentials per request
**area** clients · **category** optimization · **severity** med · **effort** M
`clients/couch-control/src/lib.rs:90-107`, `:405-409`, `:421-427`, `:348`, `:466`,
`clients/couch-control/src/streaming.rs:7-10`, `:59-67`
`Vec<u8>` DER/PKCS#8 fields render as JSON decimal arrays (~4× expansion, up to ~100 KB), and each
keypress clones them, opens a new Unix socket, writes them, and serializes them a second time just
to compare identity (with the hot path's one unguarded `.unwrap()`). Hand out an opaque endpoint
handle, keep the connection alive per handle, cache an identity hash, and base64 the binary fields.
*Report: clients-model OPT-3*

### T-057 — Fix the Hue 1 Hz spin and the Home Assistant full-state pulls
**area** clients · **category** optimization · **severity** med · **effort** M
`clients/couch-hue/src/live.rs:232-249`, `:255`, `:251-309`,
`clients/couch-ha/src/lib.rs:178-188`, `:174`, `clients/couch-ha/src/entities.rs:269`, `:288`,
`:316`, `clients/couch-voice/src/ws.rs`
One thread per Hue bridge wakes every second even while the SSE stream makes the effective interval
60 s (and has an `.expect` in a background thread, which aborts under `panic = "abort"`); listing HA
lights fetches and parses *every* entity, and an `Entities` refresh can pull `/api/states` three
times — megabytes of JSON on a quad-A7. Use `Condvar::wait_timeout` on the `dirty` flag, log instead
of `expect`, and give HA one shared state cache (ideally `subscribe_events`) that lights/covers/
climates project from.
*Report: clients-model OPT-5*

### T-058 — Use typed API responses and stop monomorphising the request future per body type
**area** web · **category** optimization · **severity** med · **effort** M
`web/couch-web/src/api.rs:142-178`, `src/screens/remote.rs:13-17`,
`src/screens/remote_device.rs:65-83`, `:88-97`, `src/screens/updates.rs:17-22`,
`src/screens/tizen.rs:94`, `src/screens/streaming_tv.rs:95`, `src/screens/activity_buttons.rs:133`
19 screens index into `Value` even where the daemon already serves a typed shape, several parsing
the same bytes twice via `from_value(...).unwrap_or_default()`; and `post/put/delete/send` are
generic over `impl Serialize` across ~60 call sites with distinct body types. Add
`ha_as<T: DeserializeOwned>` and serialise bodies at the public boundary so `send`/`parse` compile
once; measure with `tools/build-webui.sh`'s size table.
*Report: web 13*

### T-059 — Tie the CPU floor to panel state and bound the recovery backlight
**area** boot-os · **category** optimization · **severity** med · **effort** M
`stage2/gui-start.sh:80-81`, `:36-43`, `initramfs/init:76`, `recovery/init:32-35`,
`stage2/hardware-init.sh:279`, `docs/ha100-power-validation.md:93`, `:101`
The three-core HPS floor is unconditional and permanent, so an undocked remote with a dark panel
holds three cores online forever; init has already onlined all four; and recovery holds the panel
at full brightness in an unbounded loop, in exactly the state a stranded device sits in. Floor 3
while lit and 1 while dark using the existing dim/standby signal, online fewer cores at init, and
dim or time-bound the recovery backlight.
*Report: boot-os 12*

### T-060 — Read the Wi-Fi status once per tick and skip invisible properties
**area** gui · **category** optimization · **severity** low · **effort** S
`ui/couch-gui/src/main.rs:1230-1232`, `:1123-1305`, `:1394-1400`,
`ui/couch-gui/src/system.rs:49-63`, `:66-70`, `:5-18`
`wifi_level()` already calls `wifi_dbm()`, so `/proc/net/wireless` is read and parsed twice in
consecutive statements, and the dBm `format!` allocates every second regardless of change. The whole
tick also runs unchanged with the panel off (battery ×2, setup mode, approval ×2, PIN, settings
mtime, key enforcement, plus a health-file write+rename). Return `(level, dbm)` from one call and
skip the properties nobody can see while `Standby::Off`.
*Report: gui OPT-5*

### T-061 — Back off the Updates page poll
**area** web · **category** optimization · **severity** low · **effort** S
`web/couch-web/src/screens/updates.rs:89-93`, `:64-72`
The Updates screen polls `/api/updates` every 2 s unconditionally, even at `phase == "idle"`, on top
of the 15 s notification poll mounted on every page. Back off to 15 s unless `phase` is
`checking|downloading|verifying`.
*Report: web (smaller items)*

### T-062 — Cache the timezone list
**area** daemon · **category** optimization · **severity** low · **effort** S
`daemon/couch-confd/src/api/remote.rs:2-21`, `daemon/couch-confd/src/api.rs:327`
`timezones()` reads and parses three `zone.tab` files and sorts ~400 strings on every call,
including inside `PUT /api/remote` validation. One `OnceLock<Vec<String>>`.
*Report: daemon (also noted)*

### T-063 — Serve embedded assets without a linear scan and a copy
**area** daemon · **category** optimization · **severity** low · **effort** S
`daemon/couch-confd/src/assets.rs:105-108`
Assets are found by linear scan of `ASSETS` and then `.to_vec()`'d, so every request for the 380 KB
wasm allocates and copies it. Make `Asset.body` and `Reply.body` `Cow<'static, [u8]>`.
*Report: daemon (also noted)*

### T-064 — Only `create_dir_all` on writes in the hue/ha handlers
**area** daemon · **category** optimization · **severity** low · **effort** S
`daemon/couch-confd/src/api/hue.rs:26`, `daemon/couch-confd/src/api/ha.rs:22`
`create_dir_all(parent)` runs on every request including GETs.
*Report: daemon (also noted)*

---

## 4. Refactors that reduce the cost of change

### T-065 — Stop rebuilding the whole editor on every mutation
**area** web · **category** debt · **severity** high · **effort** M
`web/couch-web/src/main.rs:207-214`, `:43-52`, `:47`, `:147`, `src/api.rs:180-192`,
`src/screens/infrared.rs:188`, `src/screens/activity_buttons.rs:49`, `web/tests/browser.mjs:66-68`
`app.config.get()` is read inside the top-level reactive block, so every accepted write destroys
and rebuilds the screen subtree, discarding open `<details>`, dialogs, scroll and focus — which is
why ten pieces of per-screen UI state have migrated into the app-wide context struct (one of them,
`button_selection`, is written and never read). Key the block on `(route, revision)` or have screens
read the fields they need in their own closures, then delete the ten context fields.
*Report: web 1*

### T-066 — ✅ DONE #144 (drift test only; lists not yet unified) · One machine-readable runtime payload inventory
**area** tools-release-ci · **category** debt · **severity** high · **effort** M
`tools/release/clean_stage.py:19-20`, `:21`, `:22`, `tools/release/runtime_inventory.py:18-27`,
`:28-29`, `:30`, `:31-34`, `daemon/couch-updates/src/staging.rs:14-31`, `:21-29`, `:83`, `:89`,
`tools/build-release.sh:35-41`, `tools/build-voice.sh:31`,
`daemon/couch-updates/src/dependencies.rs:18-19`
Four hand-maintained lists in three languages describe `/opt/couch`, with no test comparing any
pair — and they already disagree in both directions (`clean_stage.BINARIES` admits six *libraries*
that are never built or shipped; `couch-mic` appears in none of the four). The licence, CGI and
script lists are duplicated the same way. Put `{binaries, scripts, cgi, licenses}` plus the build
recipe in `tools/release/ha100_runtime_payload.json`, have both Python tools and `build-release.sh`
read it, `include_str!` it into `staging.rs`, and add a test that Rust's `REQUIRED` equals the JSON.
*Reports: tools-release-ci TD-1 (daemon `couch-updates/staging.rs` is one of the four lists)*

### T-067 — One provider table: make adding an integration a single edit
**area** model · **category** debt · **severity** high · **effort** L
`model/couch-model/src/connection.rs:14-32`, `:51-69`, `:93-116`,
`model/couch-model/src/device.rs:170-205`, `buttons.rs`, `commands.rs`, `validate.rs`,
`shortcuts.rs`; `web/couch-web/src/screens/connections.rs:18-32`, `:44`, `:104-118`,
`device_picker.rs:20`, `:36-42`, `:165-166`, `:178-226`, `activity_buttons.rs:102-108`,
`activity_sequences.rs:42-48`, `activities.rs:145`, `overview.rs:60-77`;
`clients/couch-control/src/streaming.rs`; `daemon/couch-confd/src/api.rs:584-595`
Adding Tizen touched 20 files and 30 match arms — not the "seven edits" `docs/client-sdk.md`
claims — because `Provider` and `Integration` are parallel 14/16-variant enums kept in step by hand,
and because the web UI's catch-all arms (`_ => return`, `_ => return None`, an `if/else if` API
prefix chain, a 13-entry list that must agree with a 13-arm match) turn a missing provider into a
silent feature gap rather than a compile error. `GET /api/meta` already serves the daemon's
vocabulary and the browser never fetches it. Collapse the two enums, put slug/label/API segment/
discovery style/default kind/capabilities in one `const PROVIDERS` table, drive every screen from
it, replace the catch-alls with exhaustive matches, and correct the count in `docs/client-sdk.md`.
*Reports: web 2, clients-model TD-5*

### T-068 — Break up `main()`
**area** gui · **category** debt · **severity** high · **effort** L
`ui/couch-gui/src/main.rs:68-1445`, `:95-226`, `:756-1444`, `:1253-1304`, `:1123-1305`,
`:757`, `:793`, `:834`, `:936`, `:1447-1464`, `docs/code-structure.md`
One 1,378-line function owning wiring, ~130 lines of demo data, 20 controllers, ~35 callbacks and a
690-line loop with 22 mutable locals — and exactly one test, because nothing inside is callable.
Extract, in order of payoff: the demo house into `home::demo()`, the standby state machine into
`standby::Machine` (`on_key`/`on_touch`/`on_tick` → panel actions; currently zero coverage), and the
once-a-second service tick into `services::Tick`. That alone takes `main()` under 400 lines.
*Report: gui TD-1*

### T-069 — Bring the five off-broker providers into `couch-control`
**area** clients · **category** debt · **severity** high · **effort** L
`clients/couch-control/src/lib.rs:90-107`, `clients/couch-control/src/streaming.rs:195`,
`ui/couch-gui/src/activity_buttons.rs:375`, `:396-405`, `ui/couch-gui/src/tv_sonos.rs:42`,
`ui/couch-gui/src/tv_media.rs:345`, `daemon/couch-confd/src/api/sonos.rs:56`,
`daemon/couch-confd/src/api/airplay.rs:3`, `clients/couch-matter/src/lib.rs:151`,
`clients/couch-unifi-protect/src/lib.rs`, `clients/couch-appletv/src/metadata/`
The broker owns four of nine network providers; Sonos, Bluetooth HID, Apple TV metadata, Matter and
UniFi Protect each open their own long-lived transports, so none of the one-owner / lease-expiry /
credential-rotation guarantees applies to them — and the Apple TV holds two concurrent TCP sessions
from two processes while the broker's status hard-codes `now_playing_supported: false`. Add
`Spec::Sonos`, `Spec::Matter`, `Spec::Protect`, `Spec::BluetoothHid` and an `Op::AppleMetadata*` set,
fold Apple metadata into the `Client::Apple` lane, and document any deliberate exception beside Hue
and Home Assistant in `docs/control-service.md`.
*Report: clients-model TD-3*

### T-070 — Build the IR function picker from the model
**area** web · **category** debt · **severity** med · **effort** S
`web/couch-web/src/screens/infrared.rs:130`, `:275-277`, `src/screens/webos.rs:180`,
`model/couch-model/src/buttons.rs` (`functions(&Integration::Ir{..})`)
The 28 IR ids exist three times — the model's `(id, label)` pairs, a hard-coded id list in a
different order with no labels, and a third partial id→prose map — so the picker says "ok" and
"toggle" where the rest of the UI says "OK / select" and "Power toggle", and a new model function is
silently unassignable. Drive both the `<option>` list and `function_label` from `functions()`,
filtering for `power_only`, and add the webOS `"power"` alias to the model if it is really needed.
*Report: web 5*

### T-071 — Fix the web UI's repeated-control, tab and focus accessibility gaps
**area** web · **category** debt · **severity** med · **effort** S
`web/couch-web/src/screens/mod.rs:181`, `:187`, `src/screens/areas.rs:51`,
`src/screens/activity_pages.rs:24-26`, `src/main.rs:246-250`, `:212`,
`src/screens/activities.rs:109-111`, `:123`, `:140-141`, `src/screens/updates.rs:81`,
`web/couch-web/style.css:525-537`
Ten identical "Move up"/"Delete" labels per page (while `activity_pages.rs` already interpolates the
row name), no `aria-current` on the nav, activity tabs with no `tab`/`tabpanel` roles hidden with
`style:display`, a page-wide `<fieldset disabled>` that blurs focus on every save, and the
"update available" notice styled as the error banner. Add a `context` argument to the reorder and
danger buttons, the ARIA roles, a scoped busy state, and a `.banner.info` modifier.
*Report: web 7*

### T-072 — Align `color-scheme` and define the missing CSS tokens
**area** web · **category** debt · **severity** med · **effort** S
`web/couch-web/index.html:7-8`, `web/couch-web/style.css:9-16`, `:746-757`
`index.html` declares dark and `#0f1115` while the stylesheet is light `#f5f4ef`, so mobile chrome
paints dark around a cream page and native controls get dark UA defaults; and the newer activity/
page CSS references `--surface` and `--muted`, neither of which is defined anywhere, plus ~25
literal hexes. Match the meta tags to the light palette, define the two tokens, and replace the
literals with `--card`/`--line`/`--dim`.
*Report: web 9*

### T-073 — Give area reordering a scoped endpoint
**area** web · **category** debt · **severity** med · **effort** S
`web/couch-web/src/screens/areas.rs:41`, `:44`
Reordering areas does a full `PUT /api/config`, unlike every other reorder, so it is the one place
where reordering one list can clobber an unrelated concurrent edit's whole document (the `If-Match`
guard is what saves it today). Add a `PUT /api/areas` order endpoint.
*Report: web (smaller items)*

### T-074 — Give feedback when a second click is dropped
**area** web · **category** debt · **severity** med · **effort** S
`web/couch-web/src/main.rs:82-84`
`run_then` returns silently when `busy` is already set, so a second click during a save disappears
with no feedback. Queue it or toast — three lines.
*Report: web (smaller items)*

### T-075 — Document or remove the legacy `Provider::Ir` form
**area** web · **category** debt · **severity** med · **effort** S
`web/couch-web/src/screens/connections.rs:15`, `:18-32`, `:109`,
`web/couch-web/src/screens/device_picker.rs:8`
`Provider::Ir` is filtered out of both the connection list and the device-source list and absent
from the creation catalog, so `local_form(app, _, infrared = true)` is reachable only for legacy
configs. Say so in a comment, or delete it once migration is done.
*Report: web (smaller items)*

### T-076 — Name the draft icon-select wrapper
**area** web · **category** debt · **severity** med · **effort** S
`web/couch-web/src/screens/rooms.rs:143`
`ui::icon_select_signal(icon, move |_| {})` passes an empty commit closure because the device icon is
part of the Save-device draft; that reads as a bug. Add an `icon_select_draft(signal)` wrapper.
*Report: web (smaller items)*

### T-077 — Make `tools/release` a package
**area** tools-release-ci · **category** debt · **severity** med · **effort** S
`.github/workflows/firmware-watch.yml:106`, `tools/release/kernel_provenance.py:10`,
`tools/release/prepare_boot_candidates.py:14-17`, `docs/installer-review-followups.md:50-52`
Flat sibling-importing modules with no `__init__.py` forced a `sys.path.insert(0, os.getcwd())`
workaround in CI after a recorded incident, and `prepare_boot_candidates.py` silently depends on
`kernel_provenance.py`'s global path insert. Make it a package (or add a bootstrap) to remove the
class of breakage.
*Report: tools-release-ci (also noted)*

### T-078 — Align the installer crate editions
**area** tools-release-ci · **category** debt · **severity** med · **effort** S
`tools/installer/host/Cargo.toml`, `tools/installer/tui/Cargo.toml`
`host` is `edition = "2021"`, `tui` is `edition = "2024"`.
*Report: tools-release-ci (also noted)*

### T-079 — One unscoped provider entry point in `couch-confd`, then one fmt commit
**area** daemon · **category** debt · **severity** med · **effort** M
`daemon/couch-confd/src/api.rs:293-309`, `daemon/couch-confd/src/api/hue.rs:18`, `api/ha.rs:17`,
`daemon/couch-confd/src/store.rs:89-133`, `api.rs:1183-1204`
Three providers are reachable three ways — a scoped path, a shim that resolves the singleton case,
and a legacy fallback to a fixed `/opt/couch/hue-connection.json` with an env-var test hook in
production code — even though `migrate_connection_credentials` exists to move those files. The shim
also takes the store lock and allocates a `Vec<String>` on every matching request. Delete the three
`route()` functions and the env vars, make the shim the only unscoped entry (404 with "set up a
connection first"), then run `cargo fmt` over `couch-confd` alone in one commit (82 hunks / 15
files today, so `CONTRIBUTING.md`'s check is currently unmeetable for this crate).
*Report: daemon 1.7*

### T-080 — Share one provider `Endpoint` helper and merge the duplicate control panels
**area** web · **category** debt · **severity** med · **effort** M
`web/couch-web/src/screens/webos.rs:5`, `home_assistant.rs:5`, `matter.rs:7`, `hue.rs:5`,
`tizen.rs:12`, `hue.rs:59-110`, `home_assistant.rs:61-116`, plus 14 inlined copies at
`connections.rs:241`, `coreelec.rs:46-49,71-74`, `protect.rs:64-69`, `sonos.rs:52-56,74-77`,
`kodi.rs:26-31,50`, `infrared.rs:66-71,88-94,220,241`, `remote.rs:19-24`,
`remote_device.rs:72-77,99-104,123-128`, `updates.rs:23-28,43-48`, `device_picker.rs:66-71`,
`streaming_tv.rs:135-140`
The same `fail()` helper appears five times and is inlined fourteen more, along with the busy guard
and the `/api/connections/{id}/<kind>` prefix, in 19 places; and `hue::controls` and
`home_assistant::controls` are the same 50 lines twice. Add `screens::provider::Endpoint` with
`get/put/post` handling busy, the 401 → unpaired hop and the message, and collapse the two control
panels into one with a `prefix` parameter.
*Report: web 3*

### T-081 — Break up the single-line `view!` macros and the packed CSS
**area** web · **category** debt · **severity** med · **effort** M
`web/couch-web/src/screens/connections.rs:44`, `:184-186`, `device_picker.rs:82`,
`activity_sequences.rs:137-142`, `webos.rs:69`, `coreelec.rs:88`, `:94`, `:95`,
`web/couch-web/style.css:749-757`, `:761-782`
A 13-arm provider dispatch and its markup on one ~700-character line, whole submit handlers and
drag-and-drop bodies on one to three lines, nine CSS "lines" that are whole feature stylesheets —
these are the lines where bugs hide and where a one-word change is an unreviewable diff. Run
`cargo fmt`/`leptosfmt` on this crate only, pull inline async bodies into named closures, and
reformat the CSS to one declaration block per line.
*Report: web 8*

### T-082 — Put the three streaming-TV clients behind one adapter trait
**area** gui · **category** debt · **severity** med · **effort** M
`ui/couch-gui/src/tv_android.rs:89-137`, `tv_apple.rs:66-129`, `:93-98`, `tv_tizen.rs:94-176`,
`:117-134`, `:138-143`, `tv_android.rs:108-113`, `ui/couch-gui/src/tv.rs:399-705`, `:440-452`,
`:484-490`, `:496-499`, `:506-518`, `:536-544`, `:604-612`, `docs/code-structure.md`
Three near-identical 40-line connect-and-dispatch scaffolds — including the cancellation protocol,
copied three times and free to drift — plus the same five-arm send-or-error match written six times
in `worker`. Introduce `trait StreamingAdapter` with one generic `run<A>`, a Tizen `wake` hook, and
one `emit(tx, generation, result)` helper, preserving the generation checks exactly.
*Report: gui TD-6*

### T-083 — Extract the physical key routing chain and test it
**area** gui · **category** debt · **severity** med · **effort** M
`ui/couch-gui/src/main.rs:790-926` (side power `:793-816`, release `:817-820`, latency `:821-825`,
dock repeats `:826-827`, wake swallow `:832-847`, busy gate `:848`, shortcuts `:853-875`,
override `:876`, end-activity `:877-880`, menu `:881-887`, mic `:891-906`, dispatch `:913-924`),
`ui/couch-gui/src/input.rs:146-175`, `:233-298`
~140 lines where the ordering *is* the specification documented in `docs/activity-buttons.md` and
`docs/settings.md`, with zero coverage, because it lives inside `main()`. Lift it into
`fn route(app, press, state) -> Routing` returning an enum and let `main()` perform the effects,
then test the documented cases (mic on a dark panel, shortcut while the chooser is up, side power
consumed on both edges, end-activity not firing on repeat).
*Report: gui TD-8*

### T-084 — Make `Discover` real and hold one discovery socket
**area** clients · **category** debt · **severity** med · **effort** M
`clients/couch-sdk/src/discovery.rs:37`, `clients/couch-echo/src/lib.rs:236`,
`clients/couch-sonos/src/lib.rs:1101-1180`, `:1157`, `clients/couch-tizen/src/discovery.rs:14-80`,
`clients/couch-coreelec/src/discovery.rs:17-19`, `clients/couch-webos/src/lib.rs:546`,
`daemon/couch-confd/src/api/streaming_tv.rs:88`, `clients/couch-androidtv/src/lib.rs:22`,
`clients/couch-appletv/src/lib.rs:19`
The SDK's `Discover` trait has no users outside the example, while `clients/` carries four more
multicast/UDP stacks — including a hand-written mDNS name decompressor over untrusted input and two
copies of the same SSDP `M-SEARCH`. Give `Discover` `MDNS_SERVICE`/`SSDP_TARGET`, implement it in
the five clients that already publish the fact, and move the one browser into the daemon.
*Report: clients-model TD-7*

### T-085 — One source of truth for partition nodes and marker sectors
**area** boot-os · **category** debt · **severity** med · **effort** M
`tools/env.sh:18-23`, `:31-33`, `initramfs/init:18`, `:28-29`, `:44`, `:64`, `:72`, `:87`, `:146`,
`:207`, `stage2/hardware-init.sh:9`, `:13-14`, `:18`, `recovery/init:18`, `:49`, `:59`, `:67`,
`:70`, `stage2/runtime-boot.sh:7`, `tools/mkcpio.py:60-69`, `tools/flash-linux.sh:28`,
`daemon/couch-system/src/power.rs:18`, `daemon/couch-updates/src/boot.rs:25`, `tools/markers.sh:8-14`
The same constants are re-declared in ten places with prose asking the reader to keep them in step,
and the consequence is already visible: `markers.sh` prints sectors 13000-13012 only, so init's S10
verdict, S11 ttyGS major, the heartbeat (13039-13041) and recovery's R0/R1 (13020-13021) — the
markers most worth having after a failed boot — are never read. Generate one table, source it from
`tools/env.sh`, embed it via `mkcpio.py`, consume it from `power.rs`/`boot.rs`, and iterate it in
`markers.sh`.
*Report: boot-os 7*

### T-086 — Share the initramfs/recovery bring-up and the GUI health check
**area** boot-os · **category** debt · **severity** med · **effort** M
`initramfs/init:200-204` vs `recovery/init:21-25`; `initramfs/init:127-133` vs `recovery/init:39-45`;
`initramfs/init:159` vs `recovery/init:46`; `initramfs/init:185-189` vs `recovery/init:32-35`;
`initramfs/init:114-119,206-212` vs `recovery/init:58-64`; `initramfs/init:134-145`,
`initramfs/init:36`; `initramfs/boot-health.sh` + `kernel/test_boot_health.py` vs
`stage2/runtime-boot.sh:57-69`; `recovery/init:51-57`
Five blocks are copied between the two inits, and only *recovery* lacks the dynamic-`ttyGS0`-major
`mknod` fix that twelve lines of comment explain ("looks exactly like a hung kernel"), plus
`trap '' PIPE`. The GUI health check is implemented twice with different predicates and streaks
(3 vs 5, `comm` check vs `[ -d /proc/$PID ]`). Factor `initramfs/common.sh` sourced by both inits
(recovery already reuses `stage2.sh` wholesale), and have `runtime-boot.sh` call
`/extra/boot-health.sh`.
*Report: boot-os 8*

### T-087 — One boot-ramdisk assembler
**area** tools-release-ci · **category** debt · **severity** med · **effort** M
`tools/build.sh:19-29`, `tools/build-recovery.sh:26-33`,
`tools/release/prepare_boot_candidates.py:30-62`, `tools/release/prepare_public_boot.py:23`,
`tools/release/runtime_inventory.py:136`, `daemon/couch-system/src/bluetooth.rs:41`
Three hand-written assemblers of the same tree; only the release one copies `couch-bt-bridge` and
`couch-bt-hid` into `extra/`, so an image built the documented developer way boots with a Bluetooth
toggle whose binaries are missing — and `boot_inventory()`'s fourth, partial expected-member set
will not flag it. Make `clean_ramdisk(root, role)` the single implementation, call it from both
shell scripts, and derive the expected map from it.
*Report: tools-release-ci TD-4*

### T-088 — Give each client a `category()` instead of flattening errors in the broker
**area** clients · **category** debt · **severity** med · **effort** L
`clients/couch-sdk/src/error.rs:28`, `clients/couch-control/src/lib.rs:24`, `:53-88`,
`clients/couch-control/src/streaming.rs:161`, `clients/couch-ha/src/lib.rs:13`,
`couch-unifi-protect/src/lib.rs:26`, `couch-androidtv/src/lib.rs:26`, `couch-tizen/src/lib.rs:35`,
`couch-sonos/src/lib.rs:42`, `couch-hue/src/lib.rs:11`, `couch-webos/src/lib.rs:20`,
`couch-appletv/src/lib.rs:67`, `couch-coreelec/src/lib.rs:13`, `couch-kodi/src/error.rs:12`,
`couch-matter/src/lib.rs:40`, `couch-denon/src/lib.rs:13`
Sixteen error enums; the shared one is used by three crates of nineteen, and
`.map_err(|e| Error::Remote(e.to_string()))` flattens every Android TV and Apple TV error — so
`retryable()` cannot distinguish "unreachable" from "deadline missed" for those two. Rather than
forcing one enum, require each client to expose `fn category(&self) -> couch_sdk::Error` next to its
own enum and have the broker use it; start with `couch-androidtv` and `couch-appletv`.
*Report: clients-model TD-4*

### T-089 — Unify the HTTP stacks and the `ureq` pin, and measure ARM binary size
**area** clients · **category** optimization · **severity** med · **effort** L
`clients/couch-kodi/src/http.rs`, `clients/couch-tizen/src/rest.rs:65-100`,
`clients/couch-voice/src/ws.rs:106-190`, `clients/couch-ha/Cargo.toml`,
`clients/couch-hue/Cargo.toml`, `clients/couch-sonos/Cargo.toml`,
`clients/couch-unifi-protect/Cargo.toml`, `clients/couch-matter/Cargo.toml`,
`clients/couch-bt-hid/Cargo.toml`
Three HTTP implementations, two WebSocket implementations, two tokio-based crates in a workspace
whose stated rule is "no async", and a `ureq` version that is `3.4` in one crate and `=3.4.1` in
three others so two copies can link. Add a `tools/` script reporting stripped
`armv7-unknown-linux-musleabihf` size per binary so this is a number; then unify the pin and fold
`couch-tizen`'s plain-HTTP GET onto `couch-kodi`'s client moved into `couch_sdk::http`.
*Report: clients-model OPT-6*

### T-090 — Retire the duplicate Python dependency bootstrap
**area** tools-release-ci · **category** debt · **severity** med · **effort** L
`tools/release/host_dependencies.py`, `tools/release/mtk_dependencies.py`,
`tools/installer/host/src/dependencies.rs:18-20`, `:109`, `:130`, `:181`, `:193`, `:296`, `:461`,
`:533`, `:572`, `:934`, `:971`, `.github/workflows/installer-host-dependencies.yml`,
`installer-mtk-runtime.yml`, `installer-native-dependencies.yml`, `docs/installer.md:173-174`
Ten functions implemented twice (the JSON pins are shared, the logic is not); nothing shipping calls
the Python side, yet both keep dedicated workflows that download real archives on 3 and 4 OSes on
every touching PR, on top of the Rust equivalent on 4 more. Retire or move the Python to
`tools/release/legacy/`, keep the two pin files, and fold the two workflows into
`installer-native-dependencies.yml` — ~23 KB of duplicated verification code and 7 network-heavy
jobs removed.
*Report: tools-release-ci TD-8*

---

## 5. CI, release tooling & process

### T-091 — ✅ DONE #144 (Linux and macOS; Windows keeps the curated list) · Run every installer and tools test in CI
**area** tools-release-ci · **category** debt · **severity** high · **effort** S
`.github/workflows/installer-native-host.yml:51`, `installer-binaries.yml:64`,
`runtime-os-compatibility.yml:25`, `tools/installer/test_*.py` (43 files),
`tools/tests/test_build_fbcon.py`, `test_busybox_build.py`, `test_setup_mode.py`,
`test_stock_ir_hal_capture.py`
A hard-coded pattern list runs 11 of 43 installer test files; the 31 uncovered ones include the
partition write path, USB/TTY transports, the TUI, factory restore and bootstrap. The whole suite is
402 tests in 7 seconds, so there is no cost argument. Replace the pattern loop with
`unittest discover -p 'test_*.py'`, add the same for `tools/tests`, and broaden `paths:` to
`tools/installer/**`.
*Report: tools-release-ci TD-6*

### T-092 — Give the shipped workspaces CI, an ARM cross-check and a wasm PR build
**area** tools-release-ci · **category** debt · **severity** high · **effort** M
`.github/workflows/*.yml` (14 files), `pages.yml:3-5`, `CONTRIBUTING.md:66-77`,
`tools/fetch-zig.sh`, `tools/arm-cc-env.sh`
`ui/couch-gui`, `daemon/couch-confd`, `daemon/couch-system`, `web/couch-web`, `model/couch-model` and
17 of 19 client crates have no CI job at all, so a PR breaking `couch-model` merges green; no
workflow builds for `armv7-unknown-linux-musleabihf`, so an ARM link break is found during a release
cut; and wasm builds only on push to `main`, with paths that do not even include `web/**`. Add one
`workspaces.yml` matrix (`fmt` scoped to changed crates, `test --locked`, `clippy -D warnings`), one
ARM `cargo check` leg with the Zig shim, and a wasm/trunk leg.
*Report: tools-release-ci TD-5*

### T-093 — Cache, bound and de-duplicate the CI matrices
**area** tools-release-ci · **category** optimization · **severity** high · **effort** M
`.github/workflows/installer-native-host.yml:5,26-44`, `installer-native-dependencies.yml:5,18-32`,
`installer-native-frontend.yml:5,16-33`, `installer-binaries.yml:5,19-42`, `pages.yml:30`,
`appletv-client.yml:3-4`, `unifi-protect.yml:3-4`
Fifteen jobs across four workflows do an essentially identical `cargo build --release` of the same
reqwest/rustls/ring/zip crate, with `actions/cache` used exactly once in the whole tree; nine
workflows have no `concurrency:` group, two use a bare `push:` with no branch filter, four have no
`timeout-minutes`, and checkout is pinned at both `@v4` and `@v6`. Add `Swatinem/rust-cache`
everywhere, collapse the three installer workflows into one sharing an uploaded binary, and add the
missing concurrency/branch/timeout/pinning hygiene.
*Report: tools-release-ci OPT-1*

### T-094 — One source of truth for the release tag
**area** site · **category** debt · **severity** high · **effort** M
`site/index.html:73` (×2 — visible `<code>` and `data-copy`), `:77` (×2), `:80`, `README.md:29`,
`docs/installer.md:5`, `:99`, `:105`, `site/demo.js:97`, `.github/workflows/pages.yml`,
`docs/development-flow.md:50-60`
Eight literals in four files must be hand-edited per release, two of them invisible in rendered
output — so a partial edit ships a page whose visible command and copied command differ, and the
failure mode is a 404 on the project's primary call to action. Add `site/release.json`, template
`{{RELEASE_TAG}}` in `pages.yml`, add `tools/set-release.sh` covering README and `docs/installer.md`,
assert in `site-usage.cjs` that each `code` equals its `data-copy` and both carry the tag, and add
the step to the promotion checklist. Do **not** use `releases/latest` — every Couch release is a
prerelease.
*Report: site 4*

### T-095 — Fix the release build settings
**area** tools-release-ci · **category** optimization · **severity** med · **effort** S
`tools/installer/host/Cargo.toml`, `tools/installer/tui/Cargo.toml:17-19`,
`tools/build-webui.sh:29`, `:33`, `:42`, `tools/build-voice.sh:18`, `:26`,
`tools/build-release.sh:21`, `:24`, `docs/corresponding-source.md:118-123`
The installer host — the larger of the two binaries every user downloads — is the one crate with no
`[profile.release]`, so it ships unstripped with no LTO; four build invocations drop `--locked`,
including the one that builds the browser bundle into `couch-confd`, so a release can resolve a
different graph than the published lockfile; `build-webui.sh:42` cross-compiles the whole daemon
workspace; and nothing sets `--remap-path-prefix` or `SOURCE_DATE_EPOCH`, so shipped ARM binaries
embed the release host's absolute paths.
*Report: tools-release-ci OPT-2*

### T-096 — Make `fbcon` shippable by the updater, or drop the dead special cases
**area** tools-release-ci · **category** debt · **severity** med · **effort** S
`daemon/couch-updates/src/lib.rs:396-409`, `:453`, `daemon/couch-updates/src/staging.rs:71`,
`tools/release/runtime_inventory.py:26`, `:161`
`fbcon` is inventoried into the OS image, allowed by the updater and given a mode by the publisher —
but the publisher's name collection never adds it, so a runtime update can never replace it and both
special cases are dead. Add `"fbcon"` to `REQUIRED` (the `.142+` allowlist already tolerates it) or
remove both branches and say so in `docs/runtime-updates.md`.
*Report: tools-release-ci TD-2*

### T-097 — Fix the corresponding-source allowlist
**area** tools-release-ci · **category** debt · **severity** med · **effort** S
`tools/release/corresponding_source.py:22-24`, `:27-30`, `:91-101`, `:113`,
`.github/workflows/pages.yml`, `.github/workflows/installer-mtk-runtime.yml:5`
The inclusion filter silently drops anything unlisted: `CONTRIBUTING.md` (GPL-covered source the docs
point contributors at) is excluded from every published archive, the dead `AGENTS.md` entry is still
listed, `preview/` is excluded although Pages compiles and distributes it as wasm and its lockfile is
absent from `MANIFESTS`, and the retired `gui/` prototype ships. Add `CONTRIBUTING.md` and
`.gitattributes`, drop `AGENTS.md`, decide about `preview/`, and make `source_allowed` fail loudly on
an unseen top-level directory.
*Report: tools-release-ci TD-9*

### T-098 — Stop the site tests hard-coding content counts
**area** site · **category** debt · **severity** med · **effort** S
`tools/tests/site-usage.cjs:66`, `:70`, `:48`, `:56`, `:13`, `.github/workflows/pages.yml:52-64`
`count() === 9` integration cards, `count() === 5` strip items and a literal nine-slug list mean the
content changes T-132/T-133/T-136 call for fail CI until the tests are edited in lockstep — and
because `pages.yml` gates publication on them, a content-only PR breaks the deploy. Use lower bounds
or derive the expectations from a small `site/site.json`.
*Report: site 11*

### T-099 — Check a bundle against deployed updater allowlists at publish time
**area** tools-release-ci · **category** feature · **severity** med · **effort** M
`daemon/couch-updates/src/staging.rs` (`allowed()`), `tools/release/prepare_boot_candidates.py:41-45`,
`docs/runtime-updates.md`
Before `.142` the updater refused a bundle carrying any unlisted top-level name, so adding a binary
bricks updates for older remotes — which is exactly why the Bluetooth binaries are being smuggled in
through the boot ramdisk. Nothing checks this at publish time. Add
`couch-updates check-compat CLEAN_RUNTIME --against tools/release/updater_allowlists.json`, reusing
`staging::allowed()` for the current generation, and emit "installs on updaters ≥ .142 only" as a
reviewable release-notes line.
*Report: tools-release-ci FEAT-2*

### T-100 — Installer download resume and retry, plus a dev-remote smoke test
**area** tools-release-ci · **category** feature · **severity** med · **effort** M
`tools/installer/host/src/public_inputs.rs:123-181`, `:142`, `:178`,
`tools/installer/bootstrap-native.sh.in:30-39`, `tools/release/installer_launchers.py:45`,
`docs/installer.md:114-136`, `CONTRIBUTING.md:117-124`
A single `client.get(url).send()` for up to 1 GiB with no `Range` resume and no retry: a drop at
900 MB discards everything, and the launcher has the same shape with no `--retry`. The expected size
and SHA-256 are already known, which is what makes resume safe. Add `Range` resume, three bounded
retries reporting through the existing progress callback, and an opt-in
`tools/installer/acceptance/dev_remote_smoke.py` that checks `build.json`, `os-baseline.json`, the
slot symlink and the GUI heartbeat over SSH.
*Report: tools-release-ci FEAT-3*

### T-101 — `tools/release/cut.py --dry-run`
**area** tools-release-ci · **category** feature · **severity** med · **effort** L
`tools/build-release.sh`, `tools/release/runtime_inventory.py:250`, `clean_stage.py:179`,
`package_closure.py`, `prepare_rootfs.py`, `prepare_ext4.py`, `prepare_public_*.py`,
`package_public_installer.py`, `installer_launchers.py:36`, `corresponding_source.py`,
`prepare_boot_candidates.py:66`, `docs/development-flow.md:62-69`,
`docs/public-installer-packaging.md:78-83`
Fifteen commands with hand-carried paths whose order exists only in prose across four documents,
while every review gate is enforced *inside* the tools rather than by the human typing them. Add a
driver with `--dry-run` (everything except the two signing calls, plus `verify-public` and
`verify-archive`, printing the blocker list and a diff against the previous inventory) and
`--sign /private/path/runtime.seed` for the real cut on Ollie. Tagging, uploading and marking the
prerelease stay with the human.
*Report: tools-release-ci FEAT-1*

### T-102 — Decide the fate of `spike/`, `gui/` and the unreferenced `src/*.c`
**area** tools-release-ci · **category** debt · **severity** low · **effort** S
`spike/slint-fb/`, `gui/`, `tools/mkicons.sh:45`, `src/font_8x16.c`, `src/membench.c`,
`src/propcheck.c`, `docs/code-structure.md:31-34`, `tools/release/corresponding_source.py:22`,
`tools/release/test_corresponding_source.py:95`
`spike/slint-fb/` is an unbuilt, untested Cargo workspace referenced only by a doc line and a test
path exclusion; `gui/` is the retired LVGL prototype that still ships in every source archive; three
`src/*.c` files have zero references anywhere. Act on the decision `docs/code-structure.md` already
asks for: delete them, or add a one-line README saying what each is kept for.
*Report: tools-release-ci TD-9 / orientation*

### T-103 — ✅ DONE — boot-ramdisk fixture now carries `couch-bt-hid`
**area** tools-release-ci · **category** debt · **severity** high · **effort** S
`tools/release/test_prepare_boot_candidates.py:21`, `:42-43`,
`tools/release/prepare_boot_candidates.py:46-49`, `:58`
`test_clean_full_size_images_keep_distinct_kernels_and_noninstallable_state` failed with
`Missing regular input: couch-bt-hid` because the fixture still wrote only `couch-bt-bridge`.
**Fixed today by commit `4e40c0b` ("tests: boot ramdisk fixture carries couch-bt-hid") on branch
`bluetooth`** — no action required; re-run `python3 -m unittest discover -s tools/release` to confirm
125 green.
*Report: tools-release-ci TD-3*

---

## 6. Kernel

### T-104 — Guard the HA100 LED board init so it cannot kill the shared MTK LED probe
**area** kernel · **category** debt · **severity** high · **effort** S
`drivers/misc/mediatek/leds/leds_drv.c:757-759`, `leds/mt6580/leds.c:206-275`, `:1111-1112`,
`leds/mt6580/leds_hal.h:57-58` (commit `d1cecbeb`)
An unconditional `ha100_led_board_init()` in the platform-*independent* probe returns `-ENODEV`
without the HA100 DT node, so on any other board the whole MTK LED class driver — backlight
included — fails to probe; the same commit deletes the generic `MT65XX_LED_MODE_GPIO` path
tree-wide, and the prototypes live in a platform-specific header so other MTK platforms will not
link. Wrap both call sites in `IS_ENABLED(CONFIG_COUCH_HA100)`, add no-op stubs in a common header,
and make init failure non-fatal.
*Report: kernel 1.1*

### T-105 — Enable `CONFIG_COUCH_HA100`, or revert the keypad wake quirk
**area** kernel · **category** debt · **severity** high · **effort** S
`drivers/input/keyboard/matrix_keypad.c:428-431`, `drivers/misc/mediatek/Kconfig:10-15`
(commit `7c6769e2`), `kernel/couch-ha100.config`, `kernel/profiles/{normal,diagnostic}.config`,
`kernel/release-pin.json`
The symbol appears in neither the 3,899-line board config nor either profile fragment and is
`default n`, so the wake quirk is compiled out of both builds — while `release-pin.json` records it
as validated ("wake-from-standby … unchanged", which is exactly what a no-op looks like). Add
`CONFIG_COUCH_HA100=y` to the board config, rebuild, verify `power/wakeup`, then re-pin — or revert
the commit and delete the symbol.
*Report: kernel 1.2*

### T-106 — Turn off the MTK latency and lock monitors in the normal profile
**area** kernel · **category** optimization · **severity** high · **effort** S
`kernel/couch-ha100.config:1281-1285`, `:1556`, `:1658-1660`, `:1681`, `:1683-1684`, `:1687`,
`:3285`, `:3487`, `:3540`, `kernel/profiles/normal.config`
`normal.config` clears the generic lockdep set but leaves `MT_SCHED_MONITOR`, `PREEMPT_MONITOR`,
`MTK_LOCK_DEBUG`, `MTK_WQ_DEBUG`, `MTK_IRQ_NEW_DESIGN_DEBUG`, `MTK_DEBUG_LATCH`, `SCHEDSTATS`,
hotplug debug, the AEE trio and full `DEBUG_INFO` on — instrumentation of exactly the IRQ and
preempt paths whose length (the 46–62 ms keypad figure) is under investigation, so any latency
comparison is measuring the instrumentation. Move them to `diagnostic`, re-measure press-to-event,
and only then judge the three-core HPS floor.
*Report: kernel 2.1*

### T-107 — Move the EINT mux out of `mtk_gpio_to_irq()`
**area** kernel · **category** debt · **severity** high · **effort** M
`drivers/pinctrl/mediatek/pinctrl-mtk-common.c:953-971` (commit `fc3d0e69`),
`drivers/input/keyboard/matrix_keypad.c:109`, `:179`
A lookup that callers (including `disable_row_irqs()` under a spinlock with IRQs disabled) expect to
be side-effect-free now issues pinmux writes, in a file shared by mt6580/mt8127/mt8135/mt8173 with no
SoC guard, choosing the function by `strncmp(name, "EINT", 4)` rather than board data. Put the EINT
state in the `mt_gpio_kpd` node's pinctrl; if the overlay truly cannot be touched, gate on
`CONFIG_COUCH_HA100`, move it into `->irq_set_type`, bound `offset`, and match by muxval.
*Report: kernel 1.3*

### T-108 — Cut the ~540 ms panel init that `lcm_resume` repeats on every wake
**area** kernel · **category** optimization · **severity** high · **effort** M
`drivers/misc/mediatek/lcm/st7701s_wvga_dsi_vdo_boe_tn_tianxian/…:246-273`, delays at `:141`, `:144`,
`:148`, `:156`, `:160` (commit `f332af05`)
240 ms of reset sequencing plus 300 ms of in-table delay, all of it re-paid by `lcm_resume()` on
every display wake — the largest identified contributor to the ~430 ms wake-to-visible figure on a
device whose job is to answer a button press. The leading `SET_RESET_PIN(1)`/`MDELAY(20)` asserts a
level the panel is already at, and the datasheet wants 120 ms after Sleep Out and single-digit ms
around reset. Instrument first, then bisect against the datasheet, split warm resume from cold init,
and fix the stale "coe" log tag.
*Report: kernel 2.2*

### T-109 — Key the keypad wake quirk on something with binding meaning
**area** kernel · **category** debt · **severity** med · **effort** S
`drivers/input/keyboard/matrix_keypad.c:430` (commit `7c6769e2`), `tools/dtbpatch.py`,
`docs/ha100-kernel-review.md` §3
`!strcmp(np->name, "mt_gpio_kpd")` keys behaviour on the one property that carries no binding
meaning, so any overlay respin that renames the node silently drops wake. Match on `compatible`, or
better, have `dtbpatch.py` insert `linux,wakeup;` into the keypad node at image build time and drop
the kernel quirk entirely.
*Report: kernel 1.4*

### T-110 — Gate the per-transmit IR `dev_info`
**area** kernel · **category** debt · **severity** med · **effort** S
`drivers/misc/mediatek/irtx/mt6580/couch_irtx.c:252-254`, `:267` (commit `b3c10e0e`)
The one telemetry print not gated on `trace_output` fires on the success path of every IR frame —
including each frame of a held-key repeat at ~110 ms — emitting ~150 characters at `KERN_INFO` while
holding `ir->lock` and the wakelock, over a USB serial console. Gate it or demote to `dev_dbg()`,
keeping the failure-path `dev_err`.
*Report: kernel 1.5*

### T-111 — Restore the chip-global PWM 26 MHz clock select after an IR transmit
**area** kernel · **category** debt · **severity** med · **effort** S
`drivers/misc/mediatek/irtx/mt6580/couch_irtx.c:201`, `:274-278`,
`drivers/misc/mediatek/pwm/mt6580/mt_pwm_hal.c:507-517` (commit `a10b6360`),
`drivers/misc/mediatek/leds/mt6580/leds.c:722-800`
`mt_pwm_26M_clk_enable_hal(1)` is never matched by a `(0)` on any exit path, and
`PWM_CK_26M_SEL` is chip-global, not per channel — so the first IR transmission permanently changes
the clock source seen by the LED and display PWM consumers. Save and restore the bit in the `stop:`
cleanup and re-measure the backlight and display PWM around an IR burst.
*Report: kernel 1.6*

### T-112 — Replace the 6 ms of `udelay()` in LED board init
**area** kernel · **category** debt · **severity** med · **effort** S
`drivers/misc/mediatek/leds/mt6580/leds.c:258-267` (commit `d1cecbeb`)
Three 2 ms busy-spins in `probe()` while holding a mutex, in a sleepable context, against this
tree's own `Documentation/timers/timers-howto.txt`. Use `usleep_range(2000, 2500)`.
*Report: kernel 1.7*

### T-113 — Move the IR diagnostics behind a Kconfig and read the pad pin from DT
**area** kernel · **category** debt · **severity** med · **effort** S
`couch_irtx.c:32-79`, `:36`, `:163-171`, `:188-196`, `:222-235`, `:270-272`, `:315-317`,
`drivers/misc/mediatek/pwm/mt6580/mt_pwm_hal.c:398-419` (commits `146bbaeb`, `4ee1dd92`, `ce7c9ee7`)
`ir_sample_pad()` busy-polls a hardcoded GPIO 8 (with a magic `0x80000000` wrapper bit) 256 times
mid-transmission and is always linked in; `mt_pwm_dump_channel_hal()` emits register observations at
`KERN_ERR`; and the required `duration` ABI trailer is range-checked and then never used. Add
`CONFIG_COUCH_IRTX_TELEMETRY` (default n), demote the dump to `pr_info`, take the pin from the DT
node the driver already parses, and document or drop `duration` — as one binary-changing batch so it
costs a single revalidation round.
*Report: kernel 1.8*

### T-114 — Move board-hardware symbols into the board config and drop the dead subsystems
**area** kernel · **category** debt · **severity** med · **effort** S
`kernel/couch-ha100.config:1581`, `:1514`, `:1488-1489`, `:1517`, `:1524-1530`, `:1335-1336`,
`:1486`, `:2665`, `:636ff`, `:53`, `:55`, `:454`, `:47`, `:1160`, `:3208ff`, `:3215`, `:3688ff`,
`kernel/profiles/{normal,diagnostic}.config`, `drivers/misc/mediatek/connectivity/Kconfig`
(commit `7a0e5e8f`), `stage2/hardware-init.sh:22-24`, `tools/provision-alpine.sh:30`
`CONFIG_TOUCHSCREEN_COUCH_TLSC6X` and `MTK_IRTX_PWM_SUPPORT` are enabled only in the *debug* profile
fragments while the board config actively says IR is off, so a build from the board config alone has
neither touch nor IR; `MTK_COMBO_COMM_APO`'s default was flipped in Kconfig rather than declared;
and the profile carries a modem stack, two cameras with no image sensor, GPS, BT SCO without its
ALSA half, IPv6 (disabled by sysctl at boot), AUDIT, the Android binder/LMK/alarm set, ZRAM/SWAP
with no swap device, several debug symbols, and SELinux enabled with no policy. Move board symbols
into `couch-ha100.config`, declare APO explicitly, and audit the modem/camera/GPS block as one
separately-validated batch.
*Reports: kernel 1.10, boot-os 13*

### T-115 — `--check` the base config and a must-hold list, not just the fragment
**area** kernel · **category** debt · **severity** med · **effort** S
`kernel/build.sh:33-37`, `kernel/configure.py`, `kernel/couch-ha100.config:998`,
`kernel/README.md:82-83`
The merge combines a 3,899-line base with a ~15-line fragment, but the post-`olddefconfig`
verification is handed the fragment only — so if Kconfig drops a base symbol because a dependency
moved (`BT_HCIVHCI`, on which the whole Bluetooth bridge rests, or the CONSYS/WMT symbols Wi-Fi rests
on), the build succeeds and the failure appears on hardware. Add `kernel/must-hold.config` listing
what the product depends on and `--check` it too — one line in `build.sh`.
*Reports: boot-os 14, kernel 1.10*

### T-116 — Sleep the dead interval once instead of polling ~130 times
**area** kernel · **category** optimization · **severity** med · **effort** S
`drivers/misc/mediatek/irtx/mt6580/couch_irtx.c:215`, `:217`, `:219-264`,
`couch_irtx_complete.h:7-13`
The completion predicate cannot be satisfied before `actual_us` has elapsed — the comment says so —
yet the loop `usleep_range(500, 1000)`s from the first iteration: ~90–130 provably useless wakeups
per NEC frame, per key repeat, on a battery device. Sleep `actual_us` in one shot, then poll for the
residual. Better (M): request the PWM finish interrupt in `ir_probe()` and use
`wait_for_completion_timeout()`.
*Report: kernel 2.3*

### T-117 — Preallocate the IR DMA buffer
**area** kernel · **category** optimization · **severity** med · **effort** S
`couch_irtx.c:175-179`, `:280`, `:156`
`dma_alloc_coherent`/`dma_free_coherent` bracket every `write()` on the latency-sensitive path under
`ir->lock`, although the mutex already serialises writers and `IRTX_MAX_BYTES` bounds the size.
Allocate once in `ir_probe()` sized to the real maximum Couch emits (rejecting larger writes), and
keep the free in the error/remove path.
*Report: kernel 2.4*

### T-118 — Shrink the touch report read and bound the point loop
**area** kernel · **category** optimization · **severity** med · **effort** S
`drivers/input/touchscreen/mediatek/couch_tlsc6x.c:12-13`, `:24-42`, `:32`, `:51-57`, `:86-102`,
`:87`, `:197` (commits `b3028d1a`, `51a6e7f3`)
`TLSC_REPORT_SIZE` is 18 so the chunking loop does three address-then-read pairs (six I²C
transactions) per event, but the parser never reads past byte 12; and the point loop iterates both
slots unconditionally rather than by the reported finger count, so a stale second record can be
reported as a live contact. Set the size to 13, bound the loop, consider two retries instead of
three, and re-run the tap/drag tests.
*Report: kernel 2.5*

### T-119 — Establish clocks before the `ddp_pwm` register read
**area** kernel · **category** debt · **severity** med · **effort** S
`drivers/misc/mediatek/video/common/aal20/ddp_pwm.c:315-322` (commit `0d6673cd`),
`couch_irtx.c:199`
`disp_pwm_set_enabled()` reads `DISP_PWM_EN_OFF` with a comment saying "while clocks are on", but
nothing in the function establishes that — and the IR driver documents the same hazard on this SoC
("clock-gated PWM MMIO can wedge the bus"). Add the same explicit power-on, or assert the clock
state.
*Report: kernel 2.7*

### T-120 — Enable `CONFIG_DEVTMPFS` and retire the `mknod` scaffolding
**area** kernel · **category** optimization · **severity** med · **effort** M
`kernel/couch-ha100.config:1041`, `initramfs/init:141-145`, `stage2/hardware-init.sh:18`, `:29-39`,
`:63-66`, `:156`, `:161`, `:139`, `:160`, `:232`, `tools/mkcpio.py:50-69`, `kernel/README.md:134-136`
One `# CONFIG_DEVTMPFS is not set` is why the boot layer hand-manufactures device nodes in five
places (including the runtime-resolved `ttyGS0` major documented as one of "three traps that each
looked like a hang") plus three `mdev -s` sweeps. Enable `DEVTMPFS` + `DEVTMPFS_MOUNT` as its own
validated candidate, then delete the scaffolding in a follow-up, keeping the `mkcpio.py` nodes as the
pre-mount fallback.
*Report: boot-os 13*

### T-121 — Make `couch_motion` a real driver and fix the DT address
**area** kernel · **category** debt · **severity** med · **effort** M
`drivers/misc/couch_motion.c:3`, `:96-135`, `:98`, `:103`, `:108`, `:124`,
`drivers/misc/Kconfig:556-562` (commit `e581fb14`), `tools/dtbpatch.py`
A `late_initcall` that grabs I²C adapter 2 by number, synthesises a dummy client at a hardcoded
`0x27` because the DT says `0x26`, and exports a bespoke `/sys/kernel/couch_motion/` ABI with no DT
match, no `i2c_driver`, no suspend/resume, no IIO and no board condition in Kconfig. Fix the address
in the overlay via `dtbpatch.py`, convert to an `i2c_driver` with `of_match_table`, add
`depends on COUCH_HA100`, and expose it through IIO.
*Report: kernel 1.9*

### T-122 — Expose a sign-correct battery current
**area** kernel · **category** feature · **severity** med · **effort** M
`drivers/power/mediatek/battery_common.c:312`, `:356`, `:685-686`, `:1744`, `:2436-2463`,
`docs/ha100-power-validation.md:26-35`
`BAT_BatteryAverageCurrent` is `ICharging`, which is forced to 0 when not charging — so it is
structurally zero on battery and there is no discharge-current reading at all, making
"battery current in active/dim/off/suspend" unmeasurable. Add
`POWER_SUPPLY_PROP_CURRENT_NOW` (signed) and `CHARGE_COUNTER` alongside the vendor properties, and
record the unit conventions in `Documentation/couch/battery.md`.
*Report: kernel 3.1*

### T-123 — Give userspace `/dev/watchdog`
**area** kernel · **category** feature · **severity** med · **effort** M
`kernel/couch-ha100.config:2373-2375`, `:2393-2395`, `kernel/README.md` (BCB section),
`docs/ha100-kernel-review.md` §7
The hardware watchdog is enabled with a *kernel* kicker feeding it unconditionally and
`WATCHDOG_CORE` off, so it catches only a kernel that has stopped scheduling — never the failure
that matters on a remote, a hung GUI on a live kernel, which is handled by the 90 s BCB dance
instead. Enable `WATCHDOG_CORE`, register the MTK WDT, and have `MTK_WD_KICKER` hand over once
userspace opens the device so the existing GUI heartbeat feeds it.
*Report: kernel 3.2*

### T-124 — Build a real wakeup-source policy for keypad and lift
**area** kernel · **category** feature · **severity** med · **effort** M
`kernel/couch-ha100.config:557-558`, `drivers/misc/couch_motion.c`,
`drivers/input/keyboard/matrix_keypad.c:428-431`, `docs/ha100-kernel-review.md` §3
`SUSPEND` is built, autosleep and `PM_RUNTIME` are off, and the one attempted wake source is
compiled out (T-105); `couch_motion` explicitly claims no wake IRQ, so lift detection needs the CPU
already awake — the opposite of the feature. After T-105/T-109, declare the keypad EINT (and the
accelerometer if its interrupt is routed) with `device_init_wakeup()`, expose `power/wakeup`, and
verify EINT routing with `pm_test` before enabling autosleep.
*Report: kernel 3.3*

### T-125 — In-kernel STP HCI driver to replace the userspace vhci bridge
**area** kernel · **category** optimization · **severity** med · **effort** L
`Documentation/couch/bluetooth.md` task 3,
`drivers/misc/mediatek/connectivity/common/combo/linux/stp_chrdev_bt.c:76`, `:126`, `:175`, `:222`,
`:239`, `:313`, `:329`, `:356`
~250–350 lines reusing `stp_chrdev_bt.c`'s exact call sequence with the char device removed:
`hdev->open` → `mtk_wcn_wmt_func_on` + `register_event_cb`, `hdev->send` → packet-type byte +
`stp_send_data`, RX draining into `hci_recv_frame`, and the chip-reset callback marking `hdev` down.
Buys two fewer context switches and copies per packet, removes the "one bridge at a time" singleton,
and behaves correctly across CONSYS reset. Schedule after the vendor set-address command; keep
`/dev/stpbt` registered behind `CONFIG_MTK_STP_HCI` so the bridge remains a fallback.
*Report: kernel 2.6*

### T-126 — Do not take the device mutex to set a per-file IR carrier
**area** kernel · **category** debt · **severity** low · **effort** S
`couch_irtx.c:127-130`
`ir_ioctl` locks `ctx->ir->lock` to assign `ctx->carrier`, which is per-file state, so two
independent fds serialise and a carrier change blocks behind an unrelated in-flight transmit. Make
`carrier` per-device (and document it) or use `WRITE_ONCE` with a per-file lock.
*Report: kernel 2.7*

### T-127 — Claim GPIO 4 explicitly on both sides
**area** kernel · **category** debt · **severity** low · **effort** S
`drivers/misc/mediatek/lcm/st7701s_…_tianxian.c:12-13`, `:281-293`,
`drivers/input/touchscreen/mediatek/couch_tlsc6x.c:117-119` (commit `30c130c8`)
The panel's `GPIO_LCD_MAKER_ID` and the TLSC6x reset line are the same pin, claimed by neither
driver via `gpio_request()`, so nothing detects the conflict. Reading is harmless today; add
`gpio_request_one()` on both sides so a future cold probe fails loudly instead of resetting the touch
controller.
*Report: kernel 2.7*

### T-128 — Scope IR receive from the DT overlay before committing
**area** kernel · **category** feature · **severity** low · **effort** M
`docs/ha100-stock-ir-audit.md:131-163`, `tools/dtbpatch.py`,
`drivers/misc/mediatek/ir_learning/mt_irlearning.{c,h}`
Stock has `MTK_IR_LEARNING_SUPPORT` off, none of 121,470 recovered symbols names a learning driver,
and there is no verified receiver pin or evidence one is populated. The free first step is
read-only: check whether `odmdtbo.img` contains a `mediatek,irlearning-spi` node with a real pin. If
not, this is a hardware question. Do not enable the symbol speculatively — `mt_irlearning.c` shares
one buffer between SPI receive and transmit and could disturb the working TX path.
*Report: kernel 3.4*

---

## 7. Landing page & docs

### T-129 — Add Open Graph, Twitter, canonical, sitemap, robots and JSON-LD
**area** site · **category** debt · **severity** high · **effort** S
`site/index.html:3-12`, `:7`, `:8`, `site/credits.html:1`, `site/preview.html:2`,
`site/usage/*.html`, `README.md:11`, `:50-52`
No `og:`, `twitter:`, `canonical`, `robots.txt`, `sitemap.xml` or structured data anywhere, so every
Discord/Slack/X link to a project whose entire appeal is visual unfurls as a bare URL; the one
searchable string ("Sanytron HA100 custom firmware") is in the description but not the title; and
`preview.html` is indexable although rendering it costs 12.8 MB. Add the head tags on every page
(compose `og:image` from the CI-rendered `usage/screenshots/home.png`), widen the title, add a
sitemap plus robots, `noindex` the preview, and assert `og:image`/`canonical` in `site-usage.cjs`.
*Report: site 5*

### T-130 — Add a hardware section: what to buy and what it costs
**area** site · **category** feature · **severity** high · **effort** S
`site/index.html`, `README.md`, `docs/*.md`
The project's premise is that the HA100 is a cheap remote you can buy and reflash, and neither the
site nor the repo says where to get one, what it costs, what is in the box, or which revisions are
known-good — so the install section has no audience beyond people who already own the device.
*Report: site 15 (b)*

### T-131 — Link the contribute path from the site
**area** site · **category** feature · **severity** high · **effort** S
`site/index.html:63`, `CONTRIBUTING.md:8-15`, `docs/client-sdk.md`
`CONTRIBUTING.md` opens with "most of it can be worked on without owning the hardware" and points at
`docs/client-sdk.md` as the concrete path; neither is linked from anywhere on the site, whose
project-links block stops at source / kernel / issues / Discord. "Add support for your device — no
hardware needed" is the highest-leverage link the page could carry.
*Report: site 15 (c)*

### T-132 — Bring the integrations page up to all fourteen providers
**area** site · **category** debt · **severity** high · **effort** M
`site/integrations.html:2`, `site/index.html:44`, `model/couch-model/src/connection.rs:14-31`,
`:51-69`, `web/couch-web/src/screens/connections.rs:21-44`, `tools/tests/site-usage.cjs:66`
The page ships nine cards and opens with "the list below describes what is implemented today", while
the model has fourteen; Sonos, CoreELEC, Bluetooth, UniFi Protect, Matter, cameras and voice appear
nowhere in `site/`. Add the five missing cards, put Sonos on the landing strip, and generate the card
list from `Provider::label()` via a `site/integrations.json` a model test checks — then relax the
test's exact count (T-098).
*Report: site 1*

### T-133 — Put Bluetooth on the site
**area** site · **category** debt · **severity** high · **effort** M
`site/index.html:48`, `:55-61`, `site/integrations.html`, `docs/bluetooth.md:1-8`,
`docs/bluetooth-tv.md:1-35`, `:17-26`, `model/couch-model/src/connection.rs:26`,
`preview/src/demo.rs` (`documentation_screen`), `docs/site-guides.md:8-15`
`grep -ril bluetooth site/` returns nothing, although the remote is a working BLE HID peripheral, the
`BluetoothTv` provider ships with its own settings view, and "the remote *is* the Bluetooth remote
your TV expects, out of line of sight" is the clearest one-sentence differentiator the project has.
Add a features card, an integration card, `site/usage/bluetooth.html` ported from the existing prose,
a Settings→Bluetooth fixture in `documentation_screen`, and Bluetooth to the kernel card's hardware
list.
*Report: site 2*

### T-134 — Stop loading the 12.8 MB demo eagerly
**area** site · **category** debt · **severity** high · **effort** M
`site/index.html:32`, `:33`, `site/preview.html:13`, `site/demo.js:32`, `preview.html:37`,
`site/style.css:6`, `preview/Cargo.toml:24-27`, `tools/build-preview.sh:26-28`
The iframe has no `loading="lazy"` and no gating, so a phone visitor from Discord downloads
12,791,571 B of wasm plus 60 KB of glue before the hero paints — below the fold at that width — and
the reduced-motion path suppresses animation but not the download. Add `loading="lazy"`, then a
click-to-load poster using the already-rendered `usage/screenshots/home.png` (auto-load on wide
viewports, tap on narrow or with `saveData`), then a `wasm-opt -Os` step recording before/after size.
*Report: site 3*

### T-135 — Add a "does it work with my TV?" compatibility matrix
**area** site · **category** feature · **severity** high · **effort** M
`model/couch-model/src/connection.rs:51-69`, `docs/matter.md:3-5`, `docs/samsung-tizen.md`,
`docs/apple-tv-metadata.md`, `docs/bluetooth-tv.md:17-26`, `docs/ir.md:388`
The first question every visitor has, never addressed. All the data exists: brand/ecosystem × how
Couch reaches it (network / IR / Bluetooth) × what works × validation status, honestly surfacing the
experimental ones. It is also the argument for Bluetooth and IR: "no LAN API? pair over Bluetooth, or
use the 5,477-model IR library".
*Report: site 15 (a)*

### T-136 — Write the missing usage guides
**area** site · **category** feature · **severity** high · **effort** L
`site/usage/` (8 guides today), `docs/bluetooth-tv.md`, `docs/sonos.md`, `docs/voice.md`,
`docs/unifi-protect.md`, `docs/onboarding.md`, `docs/webui.md`, `docs/runtime-updates.md`,
`docs/development-flow.md:21-35`, `docs/activities.md`, `docs/activity-buttons.md`,
`preview/src/demo.rs`, `tools/tests/site-usage.cjs:13`
Missing in value order: Bluetooth, Sonos, Voice/Assist, UniFi Protect cameras, first-run/web-UI
setup, updates and channels, activities (currently explained only in passing inside `kodi.html`),
Denon, Matter. Each wants a fixture in `documentation_screen` and an expected state in
`site-usage.cjs`, per the one-renderer rule.
*Report: site 15 (g)*

### T-137 — Fix the install section's "latest" link and missing prerequisites
**area** site · **category** debt · **severity** med · **effort** S
`site/index.html:80`, `:68`, `:73`, `:77`, `docs/installer.md:14-42`, `:36-41`,
`docs/development-flow.md:12-13`
`/releases/latest` skips prereleases and every Couch release is one, so the link cannot reach the
release the commands above it install. The prose covers only "enable USB debugging, connect by USB"
and omits supported hosts, the shared Wi-Fi requirement, the disk needed for the kept Android
backup, and — the one that costs trust — the macOS administrator password prompt that arrives
seconds after "the script verifies every download". Point at the pinned tag, add the prerequisites,
and add a "read the script first" link.
*Report: site 6*

### T-138 — Fix the `#82718f` contrast failures
**area** site · **category** debt · **severity** med · **effort** S
`site/style.css:3` (`.number`, `.intro h1 em`), `:83` (`.command-label`), `:78` (`.badge`), `:2`
At 3.72:1 on `--paper`, `#82718f` fails AA on the 9 px feature numbers, the 9 px PRERELEASE badge and
the 10 px command labels — the last being the only thing distinguishing the macOS command from the
Windows one. Use `#6b5a79` (≈4.7:1) there, keep `#82718f` on `.intro h1 em` where it passes as large
text, raise `.command-label` to 11–12 px, and consider darkening `--muted`.
*Report: site 8*

### T-139 — Tame the demo live region and fix the copy confirmation
**area** site · **category** debt · **severity** med · **effort** S
`site/index.html:41`, `:73`, `site/demo.js:19-22`, `:24-30`, `:32-34`, `:43`, `:45`, `:62-65`,
`:97-100`, `site/preview.html:70`
`#demo-summary` is rewritten on every state message while an indefinite tour drives a button every
650–2800 ms, so a screen-reader user gets a polite announcement roughly every second, permanently,
unless they also have reduced motion set. And the copy button's `aria-label` pins its accessible
name, while the live region is attached at the moment of mutation — so the confirmation is never
announced, and the fallback shows `⌘C` to the Linux and Windows users the fallback exists for. Set
`aria-live="off"` while autoplaying, add a visible pause control, drop the `aria-label` in favour of
an `sr-only` span plus a persistent `role="status"`, and neutralise the shortcut hint.
*Report: site 9*

### T-140 — Add a `<noscript>` / wasm-failure poster behind the demo
**area** site · **category** debt · **severity** med · **effort** S
`site/index.html:31-35`, `site/demo.js:59`, `:61`, `site/preview.html:74`, `site/style.css:50`,
`site/usage/screenshots/home.png`, `docs/site-guides.md:17-19`, `site/README.md:7`
`#demo-loading` is hidden only by a `ready` postMessage handled in the parent, so with JS disabled or
the parent script blocked the hero's entire visual is a black rectangle reading "Loading couch."
forever — which reads as broken software. Put the CI-rendered `home.png` behind the iframe and in a
`<noscript>`; this satisfies the one-renderer rule and supplies T-134's poster and T-129's `og:image`
at no extra cost.
*Report: site 10*

### T-141 — Subset the Inter variable font
**area** site · **category** optimization · **severity** med · **effort** S
`site/style.css:1`, `site/index.html:10`, `site/assets/InterVariable.woff2` (352,240 B),
`site/assets/INTER-LICENSE.txt`
The full-coverage upstream build is preloaded on every page — 96% of the transfer on the credits
page — for a site whose entire non-ASCII inventory is thirteen code points. Subset to
`U+0000-00FF,U+2000-206F,U+2190-2193,U+2212,U+2318` keeping the 100–900 axis, or do it in
`pages.yml` from a checked-in original, and note it in `site/README.md`.
*Report: site 12*

### T-142 — Refresh `docs/site-guides.md` and the guide cross-links
**area** site · **category** debt · **severity** med · **effort** S
`docs/site-guides.md:33-34`, `:3`, `tools/tests/site-usage.cjs:13`, `:78`,
`site/index.html:17`, `:54`, `:59`, `site/usage/home-assistant.html:8`, `site/usage/infrared.html:18`
The doc says six screenshots where the pipeline makes sixteen, and instructs linking new guides from
the landing page although seven of eight guides are unreachable from `index.html`.
`home-assistant.html` is the only guide missing the `.mapping-note` caveat and `infrared.html` the
only one with no `.guide-bottom` cross-links.
*Report: site 11*

### T-143 — Correct the five stale statements in the web UI docs
**area** web · **category** debt · **severity** med · **effort** S
`docs/webui.md:585`, `:592`, `:7-9`, `:220-262`, `docs/activity-buttons.md:19-20`,
`web/couch-web/src/screens/remote.rs:40`, `src/screens/appearance.rs:24`,
`model/couch-model/src/appearance.rs:10-11`, `src/screens/connections.rs:18-32`,
`web/tests/browser.mjs:96`, `:98`, `:101`
Appearance moved to Remote settings (and `docs/remote-settings.md` already says so, so the two docs
contradict each other), the default is `#FFFFFF` not orange, the provider list is thirteen with
Infrared deliberately absent, the API table omits eight endpoint families and describes
`PUT /api/activities/{id}` without `setup`/`buttons`, and the documented Escape-clears-search
behaviour does not exist anywhere in the code.
*Report: web 6*

### T-144 — Reconcile the kernel docs with `release-pin.json`
**area** kernel · **category** debt · **severity** med · **effort** S
`couch-kernel:Documentation/couch/bluetooth.md:14-16`, `:20-22`, `kernel/README.md:3-4`, `:106-111`,
`:124`, `:128`, `:134-136`, "What works" matrix, `docs/kernel-release-candidate.md:1`, `:3`,
`docs/ha100-kernel-review.md` §4, `docs/ha100-runtime-validation.md:5`, `kernel/release-pin.json:5`,
`:13`, `kernel/patches/series`
Three documents name three different candidate commits while the pin says `06b21c74`; the in-tree
Bluetooth doc still lists `CONFIG_BT=y`/`BT_HCIVHCI=y` as pending task 1 although both shipped and
`hci0` was validated; the README's matrix still says touchscreen and IR transmit do not work,
contradicting the same file two sections later; and `patches/series` omits two commits so the patch
mirror cannot reproduce the pinned tip. One doc pass, plus a check asserting
`release-pin.json:source_commit` equals the commit named in `kernel/README.md`.
*Reports: kernel 1.11, boot-os (documentation drift)*

### T-145 — Document the three boot timers and their effect on the BCB
**area** boot-os · **category** debt · **severity** med · **effort** S
`initramfs/init:49-74`, `:109`, `stage2/runtime-boot.sh:50-82`, `:28-36`,
`docs/device-recovery.md:28-36`, `kernel/README.md:106-111`
Three independent timers act on the same boot — the 90 s + 12×5 s health gate, the 900 s dead-man,
and the update gate's 90 s / 5 heartbeats — and their ordering is safe today only because
`rollback()` explicitly clears the BCB, a comment that reads as an incident report. No one place
states the three windows and their precedence. Add a short owner/window/action/BCB-effect table to
`docs/device-recovery.md`.
*Report: boot-os 9*

### T-146 — Fix `CONTRIBUTING.md`'s workspace count and layout table
**area** tools-release-ci · **category** debt · **severity** med · **effort** S
`CONTRIBUTING.md:18`, `:21-27`, `:29-32`, `:63-64`
It says "four independent Rust workspaces", then lists five, then says "all five"; there are twelve
`[workspace]` roots in the tree (the five application ones plus `preview/`, `spike/slint-fb/`,
`tools/gemmbench/`, both installer crates and both `linux_stage` crates), and the layout table omits
`firmware/` and `initramfs/extra`.
*Report: tools-release-ci (also noted)*

### T-147 — Make the guide screenshots responsive, lazy and lighter
**area** site · **category** debt · **severity** med · **effort** M
`site/usage/usage.css:1`, `:4`, `site/style.css:3`, `site/usage/index.html:12-19`,
`tools/tests/site-usage.cjs:42`, `:52-53`
The two-column grid survives to 371 px, so each 480×800 screenshot renders at ~170×283 CSS px —
unreadable, which defeats guides whose whole job is showing the screen — while the sources are
full-resolution PNGs (`kodi.png` 411 KB) with no `srcset` and the guide index loads eight of them
eagerly (947 KB before first paint) although every other guide lazy-loads. Move the single-column
breakpoint to 700 px, add `loading="lazy"`, emit WebP in `<picture>`, and add a 240 px `srcset`
variant for the cards.
*Report: site 7*

### T-148 — Add a "what's new" releases feed
**area** site · **category** feature · **severity** med · **effort** M
`site/index.html:26`, `:66`, `docs/development-flow.md:38-46`, `.github/workflows/pages.yml`
Releases are cut frequently and the site shows only a static PRERELEASE badge. Three recent releases
with one line each tells a returning visitor the project is alive — generated at build time from one
`gh api` call, with no new maintenance surface.
*Report: site 15 (d)*

### T-149 — Add an FAQ / troubleshooting page
**area** site · **category** feature · **severity** med · **effort** M
`docs/installer-android-restore.md`, `README.md:45-46`, `docs/sonos.md:4-6`,
`docs/unifi-protect.md:6-8`, `docs/development-flow.md:21-35`, `docs/bluetooth-tv.md:11-14`
Recurring, already-answered questions that appear nowhere on the site: can I go back to Android, what
if the install fails halfway, does it need the cloud, how do updates work, what happens to Wi-Fi if I
turn Bluetooth on.
*Report: site 15 (f)*

### T-150 — Show the web UI
**area** site · **category** feature · **severity** med · **effort** M
`site/index.html:58`, `site/usage/*.html`, `web/tests/browser.mjs`, `docs/site-guides.md:8-15`
Every configuration action the site describes happens in the browser UI, and there is not one image
of it — while the device UI gets sixteen auto-rendered screenshots. `browser.mjs` already drives the
real wasm against a `--no-auth` host daemon, so the same one-renderer discipline extends to it.
*Report: site 15 (e)*

### T-151 — Delete the dead CSS and unused icons
**area** site · **category** optimization · **severity** low · **effort** S
`site/style.css:3`, `:11`, `:17`, `:23`, `:25`, `:27`, `:28`, `:30`, `:64-69`,
`site/assets/lucide/` (23 SVGs), `site/index.html:83`, `site/credits.html:1`
Seventeen of 23 `.icon-*` rules are unreferenced, six are declared twice, and six selectors match no
element — two of which (`.room-symbol`, `.scene-row`) are leftovers from an HTML mock-up of a *device
screen*, the one thing `docs/site-guides.md` explicitly forbids, so they are a trap for the next
contributor. Delete them, inline the six survivors as `data:` URIs, and keep the lucide licence.
*Report: site 14*

### T-152 — Reformat the hand-minified pages and fix `credits.html`
**area** site · **category** debt · **severity** low · **effort** M
`site/credits.html:1`, `site/integrations.html:2`, `site/usage/*.html`, `site/style.css:3`,
`site/index.html:17`, `:32`, `:45`, `site/preview.html:3`
`credits.html` is one line, `integrations.html` two, and `style.css:3` holds ~40 rules — so a
one-word copy edit is a 5 KB diff on exactly the files T-132/T-133/T-135 want to change.
`credits.html` also diverges from every other page (no skip link, no `id="main"`, no description,
favicon or theme-color, an off-pattern title) and renders a **stray duplicate brand link** below the
header. The "Codebase" nav item exists only on `index.html`, and `scrolling="no"` is obsolete and
redundant.
*Report: site 13*

---

## 8. New features (ordered by value to a universal-remote user)

### T-153 — Give Home Assistant covers and climates bindable functions
**area** model · **category** feature · **severity** high · **effort** M
`clients/couch-ha/src/entities.rs` (`CoverCommand`, `ClimateCommand`), `:57-76`,
`model/couch-model/src/validate.rs:296-304`, `model/couch-model/src/buttons.rs:294`, `:308-313`,
`model/couch-model/src/device.rs:94-105`, `ui/couch-gui/src/activity_buttons.rs`
`couch-ha` implements the complete cover and climate contracts and reads `supported_features`; the
model validates HA blinds and thermostats as first-class devices; but the button catalog returns
`&[]` for any non-`light.` entity and a test pins that — so a user can add a HA blind, see it in a
room, and find no key to map it to. This is the largest gap between what the clients can do and what
a remote user can reach. Add `Open`/`Close`/`Stop`, gate a per-domain catalog on the entity domain,
and map them in the GUI's HA arm.
*Report: clients-model FEAT-1*

### T-154 — Add value-carrying functions so dimming and volume-set are reachable
**area** model · **category** feature · **severity** high · **effort** M
`model/couch-model/src/commands.rs:9-44`, `model/couch-model/src/device.rs:259-273`,
`model/couch-model/src/buttons.rs:295-297`, `clients/couch-matter/src/lib.rs:479-493`, `:71-77`,
`clients/couch-ha/src/lib.rs:112-117`, `clients/couch-hue/src/lib.rs:5`,
`clients/couch-denon` (`Command::VolumeDb`), `clients/couch-kodi` (`set_volume`),
`clients/couch-control/src/proxies.rs:137`
`Function` has 31 fixed variants and nothing carries a level, yet five clients already implement one
and the model's dead `suggestions` advertises `dim:30`/`volume:20`/`target:18` as if they existed
(T-004). Add `Function::Level { kind, percent }` parsing `dim:`/`volume:`/`position:` 0-100, gate
`supports` per integration (and per `Light.dimmable` / `Cover.can_set_position`), and wire it through
the broker's `Op` set and the four clients.
*Report: clients-model FEAT-2*

### T-155 — Make Sonos sources, favourites and now-playing bindable
**area** clients · **category** feature · **severity** med · **effort** M
`clients/couch-sonos/src/lib.rs:105-146`, `clients/couch-sonos/src/sdk.rs:108`, `:119-133`,
`model/couch-model/src/buttons.rs:96-108`, `model/couch-model/src/commands.rs:133-145`
`SourceId`, `Source`, `NowPlaying` and `PlayModes` are all modelled and covered by 38 tests, but the
catalog is eleven transport/volume functions and `Function::supports` refuses every dynamic function
for Sonos — so "play my Sonos favourite on the green key", the single most obvious mapping a Sonos
owner wants, cannot be expressed. Accept `input:home-theater`, `input:line-in` and
`app:<favourite-id>`, implement `inputs()`/`apps()` on the existing `DeviceClient`, and surface
`NowPlaying` through the broker once Sonos moves behind it (T-069).
*Report: clients-model FEAT-3*

### T-156 — Close the Kodi, CoreELEC and Apple TV catalog gaps
**area** model · **category** feature · **severity** med · **effort** S
`model/couch-model/src/buttons.rs:109-125`, `:151-170`, `model/couch-model/src/connection.rs:95-98`,
`clients/couch-kodi/src/lib.rs:284-352`, `clients/couch-coreelec/src/lib.rs:78-103`,
`clients/couch-appletv/src/lib.rs:490-504`, `clients/couch-sdk/src/client.rs:129`
The cheapest wins in the review: Kodi's catalog has no `play`, `pause`, `rewind`, `fast-forward` or
power although the client has a generic `input(action)` reaching any `Input.*` method and JSON-RPC
has `Player.SetSpeed`/`Application.Quit`/`System.Shutdown`; CoreELEC's SSH `OsAction` is reachable
from the web UI but not a button; Apple TV has no `menu` and no `stop` although the Companion client
handles both. Extend the catalogs, adopt `DeviceClient` for `couch-kodi`, and add its
`catalog_differences` assertion so the next gap fails a test.
*Report: clients-model FEAT-4*

### T-157 — Add a low-battery warning and a Bluetooth indicator
**area** gui · **category** feature · **severity** med · **effort** S
`ui/couch-gui/src/system.rs:31-38`, `:203-218`, `ui/couch-gui/src/main.rs:1223-1229`, `:537-546`,
`:662-688`, `ui/components/status_bar.slint:11-15`, `:55-74`, `ui/app.slint:790`,
`ui/components/settings.slint:310-318`
`battery()` returns percent and charging, and both reach the UI — but nothing warns at any threshold,
nothing shows a number on battery, and nothing dims harder when the cell is nearly flat, on the
handheld state that strands the user. Bluetooth is now a first-class toggle with `on`/`starting`/
`error` and the status bar shows only clock, title, Wi-Fi and battery. Add toasts at 15% and 5%, a
numeric percent below ~20%, and a `bt` glyph bound to the existing `bt_state` property.
*Report: gui F-4*

### T-158 — Let the user choose an activity's kind
**area** web · **category** feature · **severity** med · **effort** S
`web/couch-web/src/screens/activities.rs:78`, `:129-133`, `src/screens/areas.rs:352`,
`src/screens/rooms.rs:187`, `model/couch-model/src/lib.rs:218`, `:231-248`
`ActivityKind` drives the glyph on the remote's activity strip and does not appear anywhere in the
web UI, so the same "Listen to music" activity gets a play triangle when created from the Activities
page (hard-coded `"kind":"video"`) and bars when created from a room or area — with no way to change
it. Add a two-option `<select>` beside the Room picker and in the create dialog, and drop the
hard-coded kind.
*Report: web 14*

### T-159 — Give scene steps the typed command catalog
**area** web · **category** feature · **severity** med · **effort** M
`web/couch-web/src/screens/scenes.rs:110-122`, `:131-147`, `src/screens/device_commands.rs`,
`src/screens/activity_sequences.rs:149-159`, `docs/webui.md` (Known gaps)
A scene step's command is still a bare text input seeded with the literal `"on"`, while activity
buttons, sequences and custom pages all use a searchable catalog merging model functions, assigned IR
commands and discovered `input:`/`app:` actions — so a user who types `dim:30` gets no validation and
no error until the remote tries to run it (T-004). Reuse the sequence library's dropdown, keeping a
free-text "Advanced" disclosure.
*Report: web 15*

### T-160 — Give the settings menu touch input
**area** gui · **category** feature · **severity** med · **effort** M
`ui/components/settings.slint` (zero `TouchArea`), `:158-179`, `:202`, `:204-221`, `:233`, `:249-263`
Every other interactive surface has touch areas (`home_hub` 3, `tv` 6, `keyboard` 3, …); the settings
menu has none. So on a 480×800 touchscreen a user who sees seven 72 px rows with right-arrow chevrons
— a touch affordance — taps "Display" and gets nothing, with no touch route out either. Add a
`TouchArea` per row calling the existing index-driven `activate()`, left/right hit zones on the four
stepper rows, and a back chevron.
*Report: gui F-1*

### T-161 — Add swipe paging and tap-to-open a room
**area** gui · **category** feature · **severity** med · **effort** M
`ui/screens/home_hub.slint:253`, `:320`, `:436`, `:64`, `ui/app.slint` (`dots_y`/`dots_h`),
`ui/couch-gui/src/main.rs:427`
No file under `ui/` handles a drag, so areas — the top-level paging the whole hub is built around,
with pager dots and a purpose-built memcpy slide — can only be changed with the physical keys, and
the dots are not tappable. The three hub touch targets are also inconsistent: tapping the activity
strip opens it, tapping the scenes row opens it, tapping a *room* card only moves the focus ring. Add
`open-room(i)` (already declared and wired), a horizontal-drag threshold firing `area-step(±1)` down
the existing slide path, and tappable dots.
*Report: gui F-2*

### T-162 — Show what the physical buttons do
**area** gui · **category** feature · **severity** med · **effort** M
`ui/couch-gui/src/home.rs:10`, `ui/couch-gui/src/activity_buttons.rs:60`,
`ui/couch-gui/src/main.rs:853-875`, `ui/screens/tv.slint:128`, `docs/activity-buttons.md`
The web UI has four whole screens for mapping short/long presses per activity, the remote dispatches
all of it faithfully, and the remote shows none of it — so a user holding an unlabelled remote cannot
learn that the green key is "Kodi subtitles" here and "All off" on the hub. The data is already in
memory; render it as a static list keyed by the front-panel layout, opened by a long press or as a
settings panel. No new I/O, no new worker.
*Report: gui F-3*

### T-163 — Send Bluetooth keyboard reports and read state back
**area** clients · **category** feature · **severity** med · **effort** M
`clients/couch-bt-hid/src/main.rs:46`, `:50-61`, `:426-447`, `:518-522`, `:519`, `:524`,
`ui/couch-gui/src/activity_buttons.rs:401-404`, `model/couch-model/src/buttons.rs:202-225`,
`model/couch-model/src/commands.rs:133-145`, `clients/couch-control/src/streaming.rs:188`
The HID report map declares both a keyboard and a consumer report and registers both as GATT
characteristics, but only the consumer report is ever sent — the keyboard half is advertised and
dead — and the transport is a one-way datagram returning `Outcome::default()`, so there is no
bond/connection state at all. Three increments: a `text:`/`key:` datagram walking a string into HID
usages (the dictation path the mic key wants), a request/response socket carrying bond state, then
`Spec::BluetoothHid` in the broker (T-023, T-069).
*Report: clients-model FEAT-5*

### T-164 — Configuration backup, restore and undo
**area** daemon · **category** feature · **severity** med · **effort** M
`daemon/couch-confd/src/store.rs:152-176`, `daemon/couch-confd/src/api.rs:320`, `:334`, `:335`,
`web/couch-web/src/screens/updates.rs:113`, `docs/connections.md:22`
`Store` already validates before replacing and carries a monotonic revision, and `GET`/`PUT
/api/config` are a backup/restore API — but `POST /api/config/reset` overwrites a hand-built house
with the seed and there is no way back, a bad `PUT` from a second phone is equally final, and the
Updates page warns about writing the boot partition with no offer to save the config first. Keep the
outgoing bytes as `config.<revision>.json` (newest N) inside the existing atomic path, add
`GET /api/config/history` + `POST /api/config/restore`, and add Download/Restore buttons plus an
"undo the last change" to Remote settings, noting that credentials are not included.
*Reports: daemon 3.4, web 16 (1)*

### T-165 — Wi-Fi management from the paired web UI
**area** daemon · **category** feature · **severity** med · **effort** M
`daemon/couch-system/src/network.rs:551-660`, `daemon/couch-system/src/client.rs:57`,
`daemon/couch-confd/src/api.rs:323`, `daemon/couch-confd/src/api/remote.rs:123`,
`daemon/couch-system/src/cgi.rs`
The system service exposes a full network session over `control.sock` — scan, test, save, cancel,
with a 60 s unsaved-trial expiry and rollback — and the recovery portal drives it, but the paired web
UI gets a read-only `GET /api/remote/network`. A user whose router password changed has to find the
recovery hotspot even though the remote is on their LAN and paired. Add scan and "forget a saved
network" first (no hazard), then a `POST` driving the already-tested client `Worker`, accepting that
the page loses its connection while the trial runs.
*Report: daemon 3.3*

### T-166 — Set the clock
**area** boot-os · **category** feature · **severity** med · **effort** S
`stage2/hardware-init.sh:287`, `tools/busybox/busybox.config:963`, `tools/provision-alpine.sh:30`,
`kernel/couch-ha100.config` (`RTC_HCTOSYS=y`, `RTC_SYSTOHC` unset),
`daemon/couch-confd/src/local_name.rs:19`
The device never sets its clock — no ntpd, ntpdate, hwclock or rdate anywhere — while `tzdata` is
installed with nothing to feed it and the kernel reads the RTC at boot and never writes it back. This
matters beyond timestamps: the Sonos Control API, Home Assistant over TLS and Matter commissioning
all validate certificates against wall time. Run `busybox ntpd` (already compiled in) once the lease
lands, keep it for drift, `hwclock -w` the result, put the servers in `settings.conf`, and report sync
state through `/api/health`.
*Report: boot-os 15*

### T-167 — A status page from the endpoints that already exist
**area** web · **category** feature · **severity** med · **effort** M
`daemon/couch-confd/src/api.rs:312`, `:317`, `:319`, `:584-595`,
`daemon/couch-confd/src/local_name.rs`, `web/couch-web/src/route.rs:28`, `src/main.rs:232`
`GET /api/health`, `/api/meta` and `/api/diagnostics/control` are served and never fetched by the
browser, so installed version, schema version, config path and revision, embedded asset count, the
mDNS `local_name.error` (a documented failure mode) and the broker metrics are only reachable by
`curl`. It slots beside the Updates page, which already has a route and a nav entry.
*Report: web 16 (2)*

### T-168 — Runtime slot garbage collection
**area** daemon · **category** feature · **severity** high · **effort** S
`daemon/couch-updates/src/staging.rs:277-303`, `staging::id`, `docs/runtime-updates.md`
"Completed slots are retained; automatic slot garbage collection is not yet implemented" — so every
update leaves a full copy of the runtime under `runtime/slots/<sha256>` on phone flash forever, a new
one every few days on the Dev channel. Everything needed exists: after a boot is confirmed healthy,
prune any slot that is a valid 64-hex id, is not `current`, is not either half of `pending`, and is
not the one previous slot kept for rollback. A dozen lines, and the existing fixtures make it easy to
test.
*Report: daemon 3.1*

### T-169 — Boot-image rollback and showing the installed kernel
**area** daemon · **category** feature · **severity** high · **effort** M
`daemon/couch-updates/src/boot.rs:267-278`, `:290-299`, `boot::parse`, `write_partition`,
`daemon/couch-updates/src/lib.rs:16-31`, `docs/runtime-updates.md`
`boot::activate` already saves the whole previous partition to `boot/previous.img` with a
`previous.json` describing it and records what it wrote in `installed.json` — and **nothing ever
reads any of the three**; `Status` has no field for the installed boot image, so the Updates page
cannot even show which kernel is running, and undoing one means a recovery serial shell plus a
physical Back-while-powering-on. Add `boot_version`/`boot_installed` to `Status`, then
`boot::restore()` (verify, `write_partition` with the same read-back check) behind a confirm-gated
`Request::BootRestore` and a button that appears only when `previous.img` exists.
*Report: daemon 3.2*

### T-170 — A boot diagnostics endpoint
**area** boot-os · **category** feature · **severity** med · **effort** M
`initramfs/init` (S0-S11 markers), `stage2/runtime-boot.sh:38`,
`stage2/hardware-init.sh:194`, `:270`, `:292`, `daemon/couch-confd/src/local_name.rs:19`, `:68`,
`daemon/couch-confd/src/api.rs:319`, `tools/markers.sh`
Everything a support conversation needs — the marker table, the BCB state, the active runtime slot,
the STP/association/DHCP timings — exists, but only in an eMMC scratch partition read by an adb-only
tool that skips the most useful sectors (T-085). A `/diagnostics/boot` beside the existing health and
control-metrics endpoints makes "why did it come up like this?" answerable from the web UI.
*Report: boot-os 16.1*

### T-171 — Log export
**area** boot-os · **category** feature · **severity** med · **effort** S
`tools/busybox/busybox.config:1223`, `:1225`, `stage2/system.sh`, `stage2/confd.sh`
Pairs with T-051: `syslogd` and `logread` are compiled in and unused. A ring buffer plus a "download
logs" endpoint bounds `/tmp` *and* gives the user something to send.
*Report: boot-os 16.2*

### T-172 — Factory reset
**area** boot-os · **category** feature · **severity** med · **effort** M
`daemon/couch-system/src/power.rs:23-27`, `daemon/couch-system/src/service.rs:179-187`,
`daemon/couch-system/src/access.rs`, `/opt/couch/{networks.conf,config.json,settings.conf}`,
`runtime/{current,slots,pending,attempted,previous}`, `docs/installer-factory-restore.md:67`
Nothing on-device resets state — the only "factory reset" in the tree is the installer's Android
restore. The pieces to clear are known and few, and it fits beside `power::Action` behind the same
physical-button approval the portal already uses.
*Report: boot-os 16.3*

### T-173 — Wi-Fi reconnect and roaming policy
**area** boot-os · **category** feature · **severity** med · **effort** M
`stage2/hardware-init.sh:257-268`, `:279`, `stage2/wifi-conf.sh:21-23`, `stage2/setup-mode.sh`,
`stage2/system.sh`, `stage2/confd.sh`
Association is a one-shot at boot; `wpa_supplicant` is started with no `bgscan`, nothing supervises
it, and if it dies nothing restarts it — the persistent `udhcpc` is the only self-healing part — so a
device that permanently loses its network has no path back except the GUI. Add a supervisor loop in
the shape `system.sh`/`confd.sh` already use, plus `bgscan="simple:30:-70:3600"` in the header
`wifi-conf.sh` writes.
*Report: boot-os 16.4*

---

## Suggested execution order — the first 25

| # | ID | Why now |
|---|---|---|
| 1 | T-001 | A user switching the remote on and straight off strands it in recovery; one match arm, and it is the same class of bug #138 already fixed for restart. |
| 2 | T-002 | One interrupted write permanently stops `couch-confd` starting — the web UI is gone until someone gets a shell. |
| 3 | T-003 | The deterministic fix shipped in PR #135 is dead code; the keypad-LED bug (#134) only stays fixed by luck. |
| 4 | T-004 | Scenes and activity steps save cleanly and then silently fail at press time, and the model advertises commands nothing can parse. |
| 5 | T-015 | One-line ordering fix: a runtime with no GUI currently disarms the dead-man timer. |
| 6 | T-012 | The serial shell is described three times as the only way into a wedged device, and its backup loop can never fire. |
| 7 | T-013 | Dead code that contradicts the comment justifying the wmt ordering, and reports a measurement it never made. |
| 8 | T-014 | A slow radio probe drops a configured device into onboarding; every neighbouring wait already polls for the event. |
| 9 | T-005 | A failed portal leaves the radio with neither a station nor a working AP, and leaves the CGI surface listening on the LAN. |
| 10 | T-008 | Two documented behaviours are wrong today (menu over a modal, config reload under an open screen) and one predicate fixes both. |
| 11 | T-009 | The remote's primary input silently disappears under load; every other path already reports it. |
| 12 | T-010 | The hub's designed status column never renders on real hardware — a visible feature that exists only in the demo. |
| 13 | T-011 | A failed config boot shows a convincing fake house where nothing works, with no signal the user can see. |
| 14 | T-006 | Cheap, removes duplicated BCB handling, and stops a forced reboot discarding everything dirty. |
| 15 | T-007 | One release without a digest currently disables update checking entirely, including for good releases. |
| 16 | T-023 | Any local process can drive a paired TV's power and volume; chmod plus a lib split, and it unblocks T-163. |
| 17 | T-036 | One cheap rewrite removes a syscall storm from three callers, including the settings transition and a 1 Hz GUI poll. |
| 18 | T-037 | Removes a UI freeze of up to 100 s using a pattern the file next door already documents. |
| 19 | T-038 | Deletes a flash read plus full JSON parse twice a second, forever, using infrastructure that already exists. |
| 20 | T-091 | Seven seconds of tests that currently run nowhere; catches exactly the class of failure T-103 was. |
| 21 | T-066 | Single source of truth for the payload — unblocks adding binaries and integrations without four lists drifting again. |
| 22 | T-092 | Stops PRs breaking `couch-model`/`couch-confd` from merging green, and catches ARM/wasm breakage before a release cut. |
| 23 | T-067 | One provider table turns four silent no-ops into one compile error and is the prerequisite for every integration task below. |
| 24 | T-153 | With T-067 landed, the biggest gap between what the clients already do and what a remote user can reach. |
| 25 | T-154 | Makes dimming, volume-set and blind position expressible — every client half is already written and tested. |

---

## Machine-readable table

| id | title | area | category | severity | effort |
|---|---|---|---|---|---|
| T-001 | ✅ Clear the recovery BCB on power off | daemon | debt | high | S |
| T-002 | ✅ Make an interrupted credential migration recoverable | daemon | debt | high | S |
| T-003 | ✅ Write the backlight-keeper PID after /tmp is mounted | boot-os | debt | high | S |
| T-004 | ✅ Validate scene and activity step commands | model | debt | high | S |
| T-005 | ✅ Give portal.sh a rollback trap and stop its httpd | boot-os | debt | high | M |
| T-006 | ✅ Route the update restart through power::perform | daemon | debt | med | S |
| T-007 | ✅ Skip a release with no asset digest | daemon | debt | med | S |
| T-008 | ✅ Derive the overlay predicate once | gui | debt | med | S |
| T-009 | ✅ Report a dropped activity-button command | gui | debt | med | S |
| T-010 | Publish the real room status columns | gui | debt | med | S |
| T-011 | ✅ Show an empty state instead of the demo house | gui | debt | med | S |
| T-012 | ✅ Fix the serial-shell backup respawn guard | boot-os | debt | med | S |
| T-013 | ✅ Delete the premature $PROPS extraction | boot-os | debt | med | S |
| T-014 | ✅ Wait for wlan0 instead of sleeping three seconds | boot-os | debt | med | S |
| T-015 | ✅ Move touch /tmp/stay inside the GUI-exists branch | boot-os | debt | med | S |
| T-016 | ✅ Teach release_discovery.py the real tag scheme | tools-release-ci | debt | med | S |
| T-017 | ✅ Fix wait/killall in the Wi-Fi debug supervisor | tools-release-ci | debt | low | S |
| T-018 | Consolidate the pinned-certificate TLS verifier | clients | debt | high | M |
| T-019 | Replace the nine hand-rolled credential writers | clients | debt | high | M |
| T-020 | Stop /api/health leaking interfaces when unpaired | daemon | debt | med | S |
| T-021 | Rate-limit PIN challenge minting after a burn | daemon | debt | med | S |
| T-022 | Make settings.conf writes durable and consistent | daemon | debt | med | S |
| T-023 | Chmod the bt-hid socket and split the crate | clients | debt | med | S |
| T-024 | Decode HTTP errors once in api::ha | web | debt | med | S |
| T-025 | Add browser coverage for the three largest screens | web | debt | med | S |
| T-026 | Pin and verify provision-alpine.sh downloads | tools-release-ci | debt | med | S |
| T-027 | Give the GUI supervisor crash-loop backoff | boot-os | feature | med | S |
| T-028 | Test the broker's timing, lease and dispatch | clients | debt | med | M |
| T-029 | De-shell and test the Bluetooth control paths | daemon | debt | med | M |
| T-030 | Give recovery a bounded self-escape | boot-os | debt | med | M |
| T-031 | Detect a Tizen pin mismatch from a flag | clients | debt | low | S |
| T-032 | Log refused control-socket connections | daemon | debt | low | S |
| T-033 | Zero the Wi-Fi trial journal's cleartext PSK | daemon | debt | low | S |
| T-034 | Make test scratch directories unique and cleaned | daemon | debt | low | S |
| T-035 | Stop the installer panicking on a malformed pin | tools-release-ci | debt | low | S |
| T-036 | ✅ Make process_running cheap and off GUI hot paths | daemon | optimization | high | S |
| T-037 | ✅ Move the SSH toggle and settings probes off the UI thread | gui | debt | high | S |
| T-038 | ✅ RoomMonitor reads the config snapshot | gui | optimization | high | S |
| T-039 | Type and cache the IR catalog | daemon | optimization | high | M |
| T-040 | Stop cloning the whole Config in render loops | web | optimization | high | M |
| T-041 | Cache the Sonos client instead of connecting per press | clients | optimization | high | M |
| T-042 | Cache Matter CASE sessions; read lights concurrently | clients | optimization | high | M |
| T-043 | Start the GUI in parallel with Wi-Fi bring-up | boot-os | optimization | high | M |
| T-044 | Read COUCH_REGION once | gui | optimization | med | S |
| T-045 | Remove per-frame allocations in the input path | gui | optimization | med | S |
| T-046 | Back off the wifi::status poller | daemon | optimization | med | S |
| T-047 | Stop hold_key blocking a broker lane | clients | optimization | med | S |
| T-048 | Fetch IR and discovery once per activity | web | optimization | med | S |
| T-049 | Make the icon picker lazy and memoised | web | optimization | med | S |
| T-050 | Gate the 60x per-boot dmesg snapshot loop | boot-os | optimization | med | S |
| T-051 | Bound the /tmp logs | boot-os | optimization | med | S |
| T-052 | Do not hold the store mutex across ir::save | daemon | optimization | med | S |
| T-053 | Let idle GUI workers block; slow the idle loop | gui | optimization | med | M |
| T-054 | Make discovery and Bluetooth endpoints asynchronous | daemon | optimization | med | M |
| T-055 | Stream the update download and extraction | daemon | optimization | med | M |
| T-056 | Register the broker Spec once | clients | optimization | med | M |
| T-057 | Fix the Hue spin and HA full-state pulls | clients | optimization | med | M |
| T-058 | Typed API responses; fewer monomorphisations | web | optimization | med | M |
| T-059 | Tie the CPU floor to panel state | boot-os | optimization | med | M |
| T-060 | Read Wi-Fi status once per tick | gui | optimization | low | S |
| T-061 | Back off the Updates page poll | web | optimization | low | S |
| T-062 | Cache the timezone list | daemon | optimization | low | S |
| T-063 | Serve embedded assets without a copy | daemon | optimization | low | S |
| T-064 | create_dir_all only on writes | daemon | optimization | low | S |
| T-065 | Stop rebuilding the whole editor on every mutation | web | debt | high | M |
| T-066 | ✅ One machine-readable runtime payload inventory | tools-release-ci | debt | high | M |
| T-067 | One provider table for adding an integration | model | debt | high | L |
| T-068 | Break up main() | gui | debt | high | L |
| T-069 | Bring the five off-broker providers into couch-control | clients | debt | high | L |
| T-070 | Build the IR function picker from the model | web | debt | med | S |
| T-071 | Fix repeated-control, tab and focus accessibility | web | debt | med | S |
| T-072 | Align color-scheme and define missing CSS tokens | web | debt | med | S |
| T-073 | Give area reordering a scoped endpoint | web | debt | med | S |
| T-074 | Give feedback when a second click is dropped | web | debt | med | S |
| T-075 | Document or remove the legacy Provider::Ir form | web | debt | med | S |
| T-076 | Name the draft icon-select wrapper | web | debt | med | S |
| T-077 | Make tools/release a package | tools-release-ci | debt | med | S |
| T-078 | Align the installer crate editions | tools-release-ci | debt | med | S |
| T-079 | One unscoped provider entry point, then one fmt commit | daemon | debt | med | M |
| T-080 | Share one provider Endpoint helper | web | debt | med | M |
| T-081 | Break up the single-line view! macros and CSS | web | debt | med | M |
| T-082 | One adapter trait for the three streaming TVs | gui | debt | med | M |
| T-083 | Extract and test the physical key routing chain | gui | debt | med | M |
| T-084 | Make Discover real; one discovery socket | clients | debt | med | M |
| T-085 | One source of truth for partitions and markers | boot-os | debt | med | M |
| T-086 | Share initramfs/recovery bring-up and health check | boot-os | debt | med | M |
| T-087 | One boot-ramdisk assembler | tools-release-ci | debt | med | M |
| T-088 | Per-client category() instead of flattened errors | clients | debt | med | L |
| T-089 | Unify HTTP stacks and the ureq pin; measure size | clients | optimization | med | L |
| T-090 | Retire the duplicate Python dependency bootstrap | tools-release-ci | debt | med | L |
| T-091 | ✅ Run every installer and tools test in CI | tools-release-ci | debt | high | S |
| T-092 | CI for the shipped workspaces, ARM and wasm | tools-release-ci | debt | high | M |
| T-093 | Cache, bound and de-duplicate the CI matrices | tools-release-ci | optimization | high | M |
| T-094 | One source of truth for the release tag | site | debt | high | M |
| T-095 | Fix the release build settings | tools-release-ci | optimization | med | S |
| T-096 | Make fbcon shippable or drop the dead special cases | tools-release-ci | debt | med | S |
| T-097 | Fix the corresponding-source allowlist | tools-release-ci | debt | med | S |
| T-098 | Stop the site tests hard-coding content counts | site | debt | med | S |
| T-099 | Check a bundle against deployed updater allowlists | tools-release-ci | feature | med | M |
| T-100 | Installer download resume, retry and smoke test | tools-release-ci | feature | med | M |
| T-101 | tools/release/cut.py --dry-run | tools-release-ci | feature | med | L |
| T-102 | Decide the fate of spike/, gui/ and dead src/*.c | tools-release-ci | debt | low | S |
| T-103 | DONE - boot-ramdisk fixture carries couch-bt-hid | tools-release-ci | debt | high | S |
| T-104 | Guard the HA100 LED board init | kernel | debt | high | S |
| T-105 | Enable CONFIG_COUCH_HA100 or revert the quirk | kernel | debt | high | S |
| T-106 | Turn off the MTK monitors in the normal profile | kernel | optimization | high | S |
| T-107 | Move the EINT mux out of mtk_gpio_to_irq | kernel | debt | high | M |
| T-108 | Cut the 540 ms panel init repeated on every wake | kernel | optimization | high | M |
| T-109 | Key the keypad wake quirk on a real binding | kernel | debt | med | S |
| T-110 | Gate the per-transmit IR dev_info | kernel | debt | med | S |
| T-111 | Restore the PWM 26 MHz clock select after IR | kernel | debt | med | S |
| T-112 | Replace the 6 ms of udelay in LED board init | kernel | debt | med | S |
| T-113 | Move IR diagnostics behind a Kconfig; pin from DT | kernel | debt | med | S |
| T-114 | Move board symbols into the board config | kernel | debt | med | S |
| T-115 | --check the base config and a must-hold list | kernel | debt | med | S |
| T-116 | Sleep the dead IR interval once instead of polling | kernel | optimization | med | S |
| T-117 | Preallocate the IR DMA buffer | kernel | optimization | med | S |
| T-118 | Shrink the touch report read; bound the point loop | kernel | optimization | med | S |
| T-119 | Establish clocks before the ddp_pwm register read | kernel | debt | med | S |
| T-120 | Enable CONFIG_DEVTMPFS and retire the mknods | kernel | optimization | med | M |
| T-121 | Make couch_motion a real driver; fix the DT address | kernel | debt | med | M |
| T-122 | Expose a sign-correct battery current | kernel | feature | med | M |
| T-123 | Give userspace /dev/watchdog | kernel | feature | med | M |
| T-124 | A real wakeup-source policy for keypad and lift | kernel | feature | med | M |
| T-125 | In-kernel STP HCI driver | kernel | optimization | med | L |
| T-126 | Per-file IR carrier without the device mutex | kernel | debt | low | S |
| T-127 | Claim GPIO 4 explicitly on both sides | kernel | debt | low | S |
| T-128 | Scope IR receive from the DT overlay | kernel | feature | low | M |
| T-129 | Add OG, Twitter, canonical, sitemap, robots, JSON-LD | site | debt | high | S |
| T-130 | Add a hardware section: what to buy | site | feature | high | S |
| T-131 | Link the contribute path from the site | site | feature | high | S |
| T-132 | Bring the integrations page up to fourteen providers | site | debt | high | M |
| T-133 | Put Bluetooth on the site | site | debt | high | M |
| T-134 | Stop loading the 12.8 MB demo eagerly | site | debt | high | M |
| T-135 | Add a compatibility matrix | site | feature | high | M |
| T-136 | Write the missing usage guides | site | feature | high | L |
| T-137 | Fix the install section's latest link and prerequisites | site | debt | med | S |
| T-138 | Fix the #82718f contrast failures | site | debt | med | S |
| T-139 | Tame the demo live region and copy confirmation | site | debt | med | S |
| T-140 | Add a noscript / wasm-failure poster | site | debt | med | S |
| T-141 | Subset the Inter variable font | site | optimization | med | S |
| T-142 | Refresh docs/site-guides.md and guide cross-links | site | debt | med | S |
| T-143 | Correct five stale statements in the web UI docs | web | debt | med | S |
| T-144 | Reconcile the kernel docs with release-pin.json | kernel | debt | med | S |
| T-145 | Document the three boot timers and the BCB | boot-os | debt | med | S |
| T-146 | Fix CONTRIBUTING.md's workspace count | tools-release-ci | debt | med | S |
| T-147 | Responsive, lazy, lighter guide screenshots | site | debt | med | M |
| T-148 | Add a what's-new releases feed | site | feature | med | M |
| T-149 | Add an FAQ and troubleshooting page | site | feature | med | M |
| T-150 | Show the web UI | site | feature | med | M |
| T-151 | Delete the dead CSS and unused icons | site | optimization | low | S |
| T-152 | Reformat the minified pages; fix credits.html | site | debt | low | M |
| T-153 | HA covers and climates get bindable functions | model | feature | high | M |
| T-154 | Add value-carrying functions (dim, volume, position) | model | feature | high | M |
| T-155 | Make Sonos sources and favourites bindable | clients | feature | med | M |
| T-156 | Close the Kodi, CoreELEC and Apple TV catalog gaps | model | feature | med | S |
| T-157 | Low-battery warning and Bluetooth indicator | gui | feature | med | S |
| T-158 | Let the user choose an activity's kind | web | feature | med | S |
| T-159 | Give scene steps the typed command catalog | web | feature | med | M |
| T-160 | Give the settings menu touch input | gui | feature | med | M |
| T-161 | Swipe paging and tap-to-open a room | gui | feature | med | M |
| T-162 | Show what the physical buttons do | gui | feature | med | M |
| T-163 | Bluetooth keyboard reports and state readback | clients | feature | med | M |
| T-164 | Configuration backup, restore and undo | daemon | feature | med | M |
| T-165 | Wi-Fi management from the paired web UI | daemon | feature | med | M |
| T-166 | Set the clock | boot-os | feature | med | S |
| T-167 | A status page from the existing endpoints | web | feature | med | M |
| T-168 | Runtime slot garbage collection | daemon | feature | high | S |
| T-169 | Boot-image rollback and installed-kernel display | daemon | feature | high | M |
| T-170 | A boot diagnostics endpoint | boot-os | feature | med | M |
| T-171 | Log export | boot-os | feature | med | S |
| T-172 | Factory reset | boot-os | feature | med | M |
| T-173 | Wi-Fi reconnect and roaming policy | boot-os | feature | med | M |
